# Invariant Registry

One row per rule that must never be violated. `Enforcement point` is where it is actually
prevented — not where it is documented.

Validated by `npm run audit:registries`.

| Invariant | Owner layer | Enforcement point | Test | Failure behaviour |
|---|---|---|---|---|
| A budget line always belongs to a canonical SUBCATEGORY | database | `trg_budget_lines_require_subcategory_insert/update` (migration 079) | `server/src/tests/finance-taxonomy.test.ts` | `RAISE(ABORT)` — insert rejected |
| A branch has at most one teacher and one employee payroll envelope | database | `uq_budget_lines_payroll_target` (canonical schema) | `server/src/tests/work-packages/wp01/organization-configuration.api.test.ts` | unique-constraint violation |
| Two budget lines under one subcategory may not share a name | database | `uq_budget_lines_branch_category_name` (migration 079) | `server/src/tests/finance-taxonomy.test.ts` | 409 from the API |
| A subcategory's classification equals its parent's | database | `trg_finance_categories_inherit_classification_*` (migration 077) | `server/src/tests/finance-taxonomy.test.ts` | `RAISE(ABORT)` |
| The taxonomy is exactly two levels deep | database | `trg_finance_categories_parent_is_root_*` + table CHECK (077) | `server/src/tests/finance-taxonomy.test.ts` | `RAISE(ABORT)` |
| A channel belongs to its budget line's own subcategory | database | `trg_budget_lines_channel_matches_category_*` (077) | `server/src/tests/finance-taxonomy.test.ts` | `RAISE(ABORT)` |
| A budget line balance never goes negative | database | `trg_budget_lines_nonnegative_*` (migration 065) | `server/src/tests/payroll-reversal-integrity.test.ts` | `RAISE(ABORT)` |
| A budget line holding money cannot be retired | application | `PATCH /api/finance/budget-lines/:id` | `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` | 409 naming the remaining balance |
| A retired budget line cannot be funded | application | `POST /api/finance/budget-lines/:id/charge` | `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` | 409; the treasury debit shares the transaction and rolls back |
| An instalment belongs to exactly one tuition obligation, and its sequence is unique within it | database | `uq_installment_obligation_sequence`, FK to `student_obligations` | `server/src/tests/work-packages/wp03/installment-plan-integrity.test.ts` | UNIQUE / FOREIGN KEY violation |
| A paid instalment names the payment that paid it, and cannot be paid twice | database + application | CHECK on `student_installments` + `markInstallmentPaid` guarded UPDATE | `server/src/tests/work-packages/wp03/installment-plan-integrity.test.ts` | 409; the total charged stays at one instalment |
| An instalment plan may not promise more than its term bills, and is frozen once an instalment is paid | application | `setInstallmentPlan` | `server/src/tests/work-packages/wp03/installment-plan-integrity.test.ts` | 400 naming the term's amount; 409 after the first payment |
| An allocation names exactly one funding instrument (payment XOR scholarship award) | database | CHECK on `obligation_allocations` | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | CHECK violation |
| A semester has at most one tuition obligation, and it belongs to that semester's student | database | `uq_obligation_tuition_semester`, `trg_obligations_semester_owner_insert`, `trg_obligations_student_branch_insert` | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | UNIQUE / `RAISE(ABORT)` |
| A scholarship fund may only award money it has received through allocated donations | application | `getFundPosition` consumed by `POST /api/funding/scholarships/award` | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | 409 naming received / committed / available |
| A donation may not be allocated to funds beyond its own amount | application | `fundScholarshipFromDonation` | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | 400 naming what is still unallocated |
| An award is applied only to its own student's open obligations, never beyond the award's remainder or the obligation's outstanding | application | `allocateScholarshipToObligation` | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | 403 / 400 |
| Scholarship settlement writes no cash and no ledger row | application | `server/src/core/finance/obligations.ts` writes only allocations | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | branch cash and the ledger are unchanged; reconciliation stays healthy |
| Scholarship money never reaches a student: reversal returns it to its award, closing the award returns the remainder to the fund | application | `reverseScholarshipAllocation`, `closeAward` | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | no payment row is created by either path |
| A reversal is a state change with a reason, never a deletion | database + application | `obligation_allocations.status` CHECK + reason ≥ 8 characters | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | 400 without a reason; 409 on a second reversal |
| A funded donation, an applied award and a settled obligation cannot be deleted | database | `ON DELETE RESTRICT` on `scholarship_fundings`, `obligation_allocations` | `server/src/tests/work-packages/wp07/scholarship-funding-authority.test.ts` | FOREIGN KEY violation |
| A refund names exactly one non-refund payment of the same student and branch | database | `trg_payments_refund_attribution_*`, `trg_payments_refund_target_*` | `server/src/tests/work-packages/wp07/refund-attribution-authority.test.ts` | `RAISE(ABORT)`; the route answers 400/403/404 first |
| A refund never exceeds what is left on the payment it reverses | application | `POST /api/students/:id/refund` (checked again inside the transaction) | `server/src/tests/work-packages/wp07/refund-attribution-authority.test.ts` | 400 before the write; 409 when a concurrent refund took the remainder first |
| Only a refund of a tuition charge affects the tuition position | application | `server/src/utils/studentBalance.ts` (`TUITION_PAYMENT_SQL`) | `server/src/tests/work-packages/wp07/refund-attribution-authority.test.ts` | a refunded exam/book/card fee leaves tuition unchanged |
| A term's settlement counts only money allocated to that term's own obligation | application | `server/src/core/finance/obligations.ts` (`getObligationPosition`), consumed by both payment-desk checks | `server/src/tests/work-packages/wp07/cash-allocation-authority.test.ts` | a settled term refuses further payment with 400 (409 on the in-transaction re-read) |
| A tuition refund re-opens only the semester the reversed payment settled | application | the refund inherits `payments.semester` from its target | `server/src/tests/work-packages/wp07/refund-attribution-authority.test.ts` | semester debt moves by exactly the refunded amount |
| A payment that has been refunded cannot be deleted | database | `refunds_payment_id ... ON DELETE RESTRICT` | `server/src/tests/work-packages/wp07/refund-attribution-authority.test.ts` | FOREIGN KEY violation |
| A receipt number identifies exactly one payment | database | `uq_payments_receipt_number` (canonical schema) | `server/src/tests/work-packages/wp07/document-and-authorization-integrity.test.ts` | UNIQUE violation on insert and on update |
| An invoice number identifies exactly one invoice within its branch | database | `uq_invoices_branch_invoice_number` (canonical schema) | `server/src/tests/work-packages/wp07/document-and-authorization-integrity.test.ts` | UNIQUE violation; another branch may hold the same string, matching the per-branch counter |
| A discount authorization's effective window is a real, ordered pair of calendar dates | application | `POST /api/discount-authorizations` via `server/src/utils/isoDate.ts` | `server/src/tests/work-packages/wp07/document-and-authorization-integrity.test.ts` | 400; an unparseable window would silently never activate or never expire a money grant |
| A discount grant is an explicitly stated percentage | application | `POST /api/discount-authorizations` via `assertPercent` | `server/src/tests/work-packages/wp07/document-and-authorization-integrity.test.ts` | 400 for `[10]`, `true`, `''`, `null`, exponents and hex |
| A failure to read the discount-authorization store is reported, never treated as "no authorization" | application | `server/src/core/configuration/discount-authority.ts` | `server/src/tests/work-packages/wp07/document-and-authorization-integrity.test.ts` | the error propagates; ordinary policy is not silently charged |
| Cash and savings balances never go negative | database | `CHECK` on `finance_accounts` | `server/src/tests/cash-position-reconciliation.test.ts` | constraint violation |
| Total branch liquidity after an owner withdrawal remains at or above six months of fixed costs | application transaction + database balance checks | `server/src/core/finance/profit-distribution.ts` ceiling + post-debit assertion in `server/src/routes/bos.routes.ts` | `server/src/tests/bos-profit-withdrawal-integrity.test.ts` | 409 and the transaction rolls back without a drawing or cash mutation |
| Period owner drawings never exceed the owner-approved tier allowance, available main cash, or liquidity above reserve | application transaction | `server/src/core/finance/profit-distribution.ts`, recomputed under the withdrawal transaction in `server/src/routes/bos.routes.ts` | `server/src/tests/bos-period-authority.test.ts`, `server/src/tests/bos-profit-withdrawal-integrity.test.ts` | 409; rejected and concurrent losing requests move no money |
| The published profit-withdrawal ceiling is always the current accounting-month ceiling enforced by withdrawal | application | shared `currentProfitDistributionPosition` input loader + `computeProfitDistribution` | `server/src/tests/bos-period-authority.test.ts` | non-month calculation requests return 400; enforcement recomputes under its write transaction |
| Capital expenditure is never operating expense | application | `server/src/core/finance/ledger-classification.ts` | `server/src/tests/finance-accounting-classification.test.ts` | excluded from the P&L expense total |
| Non-expense cash movements are never operating expense | application | `server/src/core/finance/ledger-classification.ts` | `server/src/tests/finance-accounting-classification.test.ts` | excluded from the P&L expense total |
| An uncategorised expense still counts as operating cost | application | `COALESCE(..., 'operating_expense')` in `ledger-classification.ts` | `server/src/tests/finance-accounting-classification.test.ts` | conservative default; never silently dropped |
| Owner drawings are not revenue and not operating cost | application | `ledger-classification.ts` (`OWNER_DRAWINGS_CATEGORY_ID`) | `server/src/tests/ledger-classification-consistency.test.ts` | reported under `transfers` |
| Owner capital injection is not revenue | application | `ledger-classification.ts` (`CAPITAL_INJECTION_CATEGORY`) | `server/src/tests/ledger-classification-consistency.test.ts` | reported under `transfers` |
| `SUM(budget_lines.current_amount) = SUM(budget_charge) − SUM(budget-funded expense)`, where a budget movement is SIGNED | application | `server/src/core/finance/budget-movements.ts` (writer) + `server/src/utils/reconciliation.ts` (check) | `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts`, `server/src/tests/cash-position-reconciliation.test.ts` | `budgetVariance ≠ 0` → unhealthy |
| A budget movement and its ledger row are written together, inside a transaction, to the branch that owns the line | application | `server/src/core/finance/budget-movements.ts` | `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` | `Error` outside a transaction; 409 when the line cannot cover the movement |
| `budget_lines.allocated_amount` equals the net money placed in the line, so `allocated − current` is spend | application | `server/src/core/finance/budget-movements.ts` | `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` | utilization would misreport a returned line as fully used |
| `saving_transfer` rows describe the branch savings account and nothing else | application | `server/src/utils/income.ts` is the only writer | `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` | `savingVariance ≠ 0` → unhealthy |
| The configured savings rate is inside 0–100 whenever income is recorded | application | `PUT /api/finance/saving-engine/settings` (write) and `server/src/utils/income.ts` (read) | `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` | 400 on write; 409 on income, never a silent substitute rate |
| Branch cash = operating income − savings − owner drawings | application | `server/src/utils/reconciliation.ts` | `server/src/tests/cash-position-reconciliation.test.ts` | `cashVariance ≠ 0` → unhealthy |
| Every completed payment has a ledger row and vice versa | application | `server/src/utils/reconciliation.ts` | `server/src/tests/financial-integrity.test.ts` | `unmatchedPayments`/`orphanLedgerRows` |
| A payroll advance may exceed earned salary only for employees | application | `server/src/routes/teachers.routes.ts` | `server/src/tests/employee-payroll-idempotency.test.ts` | teacher `advance` → 400 |
| Payroll is idempotent per live (person, period, amount, type) event, including a retry straddling the derived-key boundary | database + application | posted-only `uq_teacher_salary_idempotency` / `uq_employee_salary_idempotency`; both payroll routes consume `resolveIdempotency().candidates`; teacher ledger notes preserve the replay settlement snapshot | `server/src/tests/work-packages/wp08/payroll-certification.attack.test.ts` | one payment, one ledger fact and one envelope debit; a teacher replay returns its original due/prior/remaining position; mismatched explicit key is 409 |
| A voided payroll event is retained history, never a live replay; a correction posts a new live fact and signed contra | database + application transaction | posted-only idempotency indexes; void endpoints plus salary-ledger immutable-transition triggers | `server/src/tests/work-packages/wp08/payroll-certification.attack.test.ts`, `server/src/tests/work-packages/wp08/payroll-certification.review.test.ts` | void is 200 once; a correction debits once; second void is 409 |
| Every salary ledger fact has a positive whole-AFN amount, canonical Shamsi period and matching expense transaction; payroll facts and their linked transactions cannot be mutated or deleted | database | payroll fact / immutable / financial-transaction guard triggers in `server/src/db/schema.sql` | `server/src/tests/work-packages/wp08/payroll-certification.attack.test.ts`, `server/src/tests/work-packages/wp08/payroll-certification.review.test.ts` | direct malformed, orphan, rewrite or delete is `RAISE(ABORT)` |
| An active malformed payroll rule blocks calculation and payment rather than substituting neutral policy | application | `PayrollRuleConfigurationError` mapping in `server/src/routes/teachers.routes.ts` | `server/src/tests/work-packages/wp08/payroll-certification.attack.test.ts` | 409 and no debit, transaction or ledger row |
| An employee transfer requires authority over both source person and target branch | application | `POST /api/employees/:id/transfer` uses `requireEmployee` plus `canAccessBranchResource` | `server/src/tests/work-packages/wp08/payroll-certification.attack.test.ts` | 403; employee and linked account remain on the source branch |
| Every idempotency replay is scoped to the business event the request describes (invoice+student, book+branch, donor+campaign+amount, person+period) | application | `invoices.routes.ts`, `books.routes.ts`, `funding.routes.ts`, `teachers.routes.ts` | `server/src/tests/idempotency-replay-scope.test.ts` | 409 when the key belongs to a different event |
| An invoice payment replay is scoped to the invoice and student it was recorded on | application | `POST /api/invoices/:id/pay` (pre-check, in-transaction re-check and unique-violation branch all use one scoped lookup) | `server/src/tests/work-packages/wp07/invoice-payment-boundary.test.ts` | 409 when the key belongs to a different payment; 200 replay only for the same invoice |
| A payment method is one of cash / card / bank transfer, or the request is refused | application | `POST /api/invoices/:id/pay`, `POST /api/students/:id/payments` | `server/src/tests/work-packages/wp07/invoice-payment-boundary.test.ts` | 400; nothing is recorded as cash by default |
| A user cannot read or write another branch's resources | application | `server/src/middleware/auth.ts` (`resolveBranchScope`, `canAccessBranchResource`) | `server/src/tests/work-packages/wp01/organization-configuration.dependent-scope.test.ts` | 403/404 without disclosure or mutation |
| Campus, partner and system-setting administration requires an organization-scoped Owner assignment | application | `server/src/middleware/auth.ts` (`requireGlobalOwner`) | `server/src/tests/work-packages/wp01/organization-configuration.authorization.test.ts` | 403 before mutation |
| A system-setting snapshot labels branch cash with the one existing branch explicitly selected by the organization Owner | application | `server/src/routes/settings.routes.ts` | `server/src/tests/work-packages/wp01/organization-configuration.authorization.test.ts` | unknown/malformed branch rejected; no unidentified cash balance returned |
| Total partner shares never exceed 100%, and every individual share is within 0–100% | database + application | `partners` CHECKs + `trg_partners_total_share_*` + `server/src/routes/branches.routes.ts` | `server/src/tests/work-packages/wp01/organization-configuration.integrity.test.ts` | 400 or constraint abort without partial write |
| A generic rule definition and every new version commit atomically; malformed active rules fail closed rather than being skipped | application transaction + database | `server/src/core/configuration/rule-engine.ts`; JSON/boolean/version CHECKs in `server/src/db/schema.sql` | `server/src/tests/work-packages/wp01/organization-configuration.integrity.test.ts` | validation error, explicit corruption failure or transaction rollback |
| A catalog child belongs to its declared program version and branch | application | relationship guards in `server/src/routes/catalog.routes.ts` and `server/src/core/academic/catalog-service.ts` | `server/src/tests/work-packages/wp01/organization-configuration.api.test.ts`, `server/src/tests/work-packages/wp01/organization-configuration.authorization.test.ts` | 400/403 before write or evaluation |
| Catalog scalar inputs and fee-snapshot outputs are finite, typed and canonical before they become configuration or money | application | `server/src/routes/catalog.routes.ts`, `server/src/core/academic/catalog-service.ts` | `server/src/tests/work-packages/wp01/organization-configuration.api.test.ts` | HTTP 400; malformed input/storage produces no catalog write or fee snapshot |
| Class generation cannot mix a run branch with another branch's version, term, offering, slot or room, exceed current room/capacity coherence, or publish malformed capacity, fee, gender or run state | application transaction | `server/src/core/academic/class-generation-engine.ts` | `server/src/tests/work-packages/wp01/organization-configuration.integrity.test.ts` | preview/publish rejected; draft transaction rolls back |
| Permanent branch deletion cannot discard cash or leave provisioning orphans | application transaction | `server/src/routes/branches.routes.ts` | `server/src/tests/work-packages/wp01/organization-configuration.api.test.ts` | non-zero account/envelope returns 409; zero-balance account and envelopes delete atomically with the branch |
| Requester and approver of an expense must differ | application | `server/src/routes/finance.routes.ts` | `server/src/tests/finance-expense-request-integrity.test.ts` | 403 |
| Permissions come only from a live `user_roles` assignment | application | `server/src/core/rbac/rbac-service.ts` (`resolveUserPermissions`) | `server/src/tests/rbac-single-authority.test.ts` | principal resolves to zero permissions |
| Deleting a user's assignments revokes their access | application | `server/src/core/rbac/rbac-service.ts` | `server/src/tests/work-packages/wp02/rbac-home-branch-invariant.test.ts` | guarded endpoint returns 403 |
| There is no role column on `users` at all | database | `server/src/db/schema.sql` | `server/src/tests/rbac-single-authority.test.ts` | `no such column: role` |
| The session token carries no role or permission claim | application | `server/src/utils/auth.ts` (`TokenPayload`) | `server/src/tests/rbac-single-authority.test.ts` | a forged claim is ignored; authority is re-resolved per request |
| `users.branch_id` never authorizes branch access | application | `server/src/core/rbac/rbac-service.ts` (`canAccessBranch`) + `server/src/middleware/auth.ts` (`resolveBranchScope`) | `server/src/tests/work-packages/wp01/organization-configuration.authorization.test.ts` | 403 when no live assignment authorizes the identity branch |
| Reading a notification never marks it read for another user | database + application | composite PK on `notification_read_receipts` + authenticated user id in `server/src/routes/audit.routes.ts` | `server/src/tests/notifications-authority.test.ts` | one idempotent receipt for the caller; every other viewer remains unread |
| An expired assignment grants nothing | application | `server/src/core/rbac/rbac-service.ts` | `server/src/tests/work-packages/wp02/rbac-expired-grant-escalation.test.ts` | 403 |
| A user has at most one primary role assignment | database | `trg_user_roles_single_primary` | `server/src/tests/rbac-single-authority.test.ts` | `SqliteError: user may have only one primary role` |
| Authorization guards name canonical role codes only | application + compiler | `RoleCode` derived from `ROLE_CODES` in `server/src/core/rbac/permission-catalog.ts` | `server/src/tests/work-packages/wp02/rbac-scope.test.ts` | unknown role name fails to compile |
| A workflow step naming an unknown role is reported, not silently denied | application | `server/src/routes/workflows.routes.ts` (`assertKnownStepRole`) | `server/src/tests/event-bus.test.ts` | 409 naming the bad role and step |
| Every stored amount is a whole number of AFN | database | INTEGER money columns + `trg_*_money_scale_*` | `server/src/tests/money-boundary-property.test.ts` | `must be a whole number of AFN` |
| Operator-supplied money is never silently rounded | application | `server/src/utils/money.ts` (`assertMoney`) | `server/src/tests/money-boundary-property.test.ts` | HTTP 400 |
| A single amount cannot exceed MAX_SAFE_INTEGER/100, so aggregates stay exact | application | `server/src/utils/money.ts` (`MAX_MONEY`) | `server/src/tests/money-boundary-property.test.ts` | HTTP 400 |
| No application code uses a physical direction utility | build | `scripts/verify-design-system.mjs` | provoked: `ml-4`/`pr-2`/`text-left` reported with their logical replacements | release gate fails |
| Only the DirectionProvider pins text direction | build | `scripts/verify-design-system.mjs` | provoked: a view setting `dir="rtl"` is reported | release gate fails |
| A report names metric ids and never carries its own SQL | application | `server/src/core/reporting/report-engine.ts` | `server/src/tests/reporting-catalog.test.ts` | unknown metric throws, never returns 0 |
| The same metric yields the same value in every report | application | `METRIC_CATALOG` single definition | `server/src/tests/reporting-catalog.test.ts` | two reports compared directly |
| Branch scope is applied by the engine, not by each metric | application | `server/src/core/reporting/report-engine.ts` | `server/src/tests/reporting-catalog.test.ts` | cross-branch totals diverge |
| A report's own permission is enforced on top of Report.View | application | `server/src/routes/reports.routes.ts` | `server/src/tests/reporting-catalog.test.ts` | 403 |
| Every reporting surface resolves a period through the calendar authority | application | `server/src/core/calendar/periods.ts` | `server/src/tests/reporting-catalog.test.ts` | overview and engine spans compared directly |
| A historical period is a Shamsi key, not a Gregorian window | application | `periodBoundariesForKey` | `server/src/tests/reporting-forensic.test.ts` | 400 on an unrecognised key |
| Every required report category has at least one declared report | application | `REPORT_CATALOG` | `server/src/tests/reporting-catalog.test.ts` | provoked: removing the teacher report names it |
| The overview and the report engine agree on operating income and expense | application | shared `ledger-classification` predicates | `server/src/tests/reporting-catalog.test.ts` | provoked: capital injection leaking into income gives 48900 vs 8900 |
| A capital-expenditure row never lands in operating expense | application | `finance_categories.classification` | `server/src/tests/reporting-catalog.test.ts` | capex rises, opex unchanged |
| A printed document declares paper size and margins | build + test | `src/design-system/print.ts` | `server/src/tests/print-document.test.ts` | no `@page` in output |
| Printed tables repeat their headers across pages | build + test | `src/design-system/print.ts` | `server/src/tests/print-document.test.ts` | page two is unlabelled |
| A printed document never fixes text to a physical side | build + test | `src/design-system/print.ts` | `server/src/tests/print-document.test.ts` | `text-align:left/right` absent |
| No component opens its own print window | build | `scripts/verify-design-system.mjs` | provoked: a new `window.open('')` is reported | release gate fails |
| No service module writes to console directly | build | `scripts/verify-logging.mjs` | provoked: a `console.log` in a route is reported | release gate fails |
| A credential-shaped field is never written to a log | application | `server/src/core/observability/logger.ts` (`redact`) | `server/src/tests/logging-authority.test.ts` | value replaced with `[redacted]` |
| A log line keeps the caller's message even when an error is attached | application | `describeError` uses `errorMessage` | `server/src/tests/logging-authority.test.ts` | message would be overwritten by the exception text |
| A successful automated backup always has hash-identical, SQLite-valid local and required external copies | application + filesystem | `server/src/core/operations/database-backup.ts` (online backup, integrity/FK checks, SHA-256/size comparison, publish-after-verify) | `server/src/tests/database-backup.test.ts` | incomplete files are removed; startup throws or runtime health/readiness becomes 503 |
| Automated backup retention never exceeds 7 daily, 4 weekly and 12 monthly verified pairs | application + filesystem | paired GFS pruning in `server/src/core/operations/database-backup.ts` | `server/src/tests/database-backup.test.ts` | unmatched/expired managed files are deleted from both destinations; deletion failure fails the run |
| A verified database backup is never more than 24 hours old while the backend remains healthy | application scheduler + readiness | startup freshness check and 24-hour scheduler in `server/src/core/operations/database-backup.ts`; health integration in `server/src/index.ts` | `server/src/tests/database-backup.test.ts` | missing/stale startup pair triggers backup; failure prevents/degrades readiness |
| An explicit permission deny dominates grants deterministically | application | `server/src/core/rbac/rbac-service.ts` (`resolveUserPermissions`) | `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | denied code is absent regardless of override row order |
| A permission and its branch reach come from the same live assignment | application | `canAccessBranchForRequirement` | `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | cross-position scope laundering returns 403 |
| An `own` or `class` action scope never becomes branch-wide merely because its assignment reaches the branch | application | `hasPermissionForBranchWithActionScopes` plus object predicates in RBAC consumers | `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | unrelated same-branch collections, sessions, students, exams, and attendance return no data or 403 |
| Teacher academic reads and writes are limited to the linked teacher's classes, students, sessions, and attendance | application | `server/src/core/rbac/abac.ts` and filtered route consumers | `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | own objects remain usable; colleague objects are absent or refused |
| `Student.View` alone never exposes tuition, discounts, installments, balances, or financial journey events | application | `server/src/routes/students.routes.ts`, `server/src/routes/journey.routes.ts` | `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | financial fields are omitted without `Payment.View` |
| Account takeover authority cannot be assembled across positions or branches | application | `canAdministerUser`, `users.routes.ts`, `security.routes.ts` | `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | mutation returns 403 and session state is unchanged |
| Each linked student, teacher, employee, or partner has at most one account | database | partial unique indexes on `users.linked_*_id` | `server/src/tests/work-packages/wp02/identity-access.architecture.test.ts` | duplicate link fails at the database boundary |
| Teacher and employee account links exist only on `users` | database + application | `server/src/db/schema.sql`, `server/src/routes/teachers.routes.ts` | `server/src/tests/work-packages/wp02/identity-access.architecture.test.ts` | entity tables have no `user_id` column |
| Student portal accounts have one linked student, a primary student role, no staff role, and no effective staff permission | application + database | `server/src/routes/users.routes.ts`, `server/src/routes/security.routes.ts`, `server/src/routes/auth.routes.ts` | `server/src/tests/work-packages/wp02/student-portal-privilege-boundary.test.ts`, `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | provisioning or login fails closed |
| A stale logout token cannot revoke a newer session | application | conditional `session_version` update in `server/src/routes/auth.routes.ts` | `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | stale logout clears its cookie but leaves the current version unchanged |
| Frontend global-Owner controls use the server-resolved fact, never a role label | API + UI | `isGlobalOwner` in auth responses and `AuthUser` | `server/src/tests/work-packages/wp02/identity-access.architecture.test.ts` | scoped Owner controls remain hidden; source scan rejects label bypasses |
| Student phone identity, including a converting lead's phone, is syntactically valid and unique under the canonical normalized key | database + application | normalized index and syntax triggers in `server/src/db/schema.sql`; `studentPhoneKey` and `assertStudentPhoneSyntax` in `server/src/core/students/student-input.ts`; visitor intake/conversion guards | `server/src/tests/work-packages/wp03/student-subsystem-remediation.test.ts`, `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | 400/409 or database abort; no duplicate profile is written |
| One Tazkira identity cannot belong to unrelated visitor and student records | database + application | cross-table Tazkira triggers plus student/visitor write guards | `server/src/tests/work-packages/wp03/visitor-subsystem-audit.test.ts`, `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | 409 or database abort; conversion may preserve only its linked lead identity |
| Graduation is terminal and releases active enrollment seats and semesters | application transaction | `server/src/core/students/student-lifecycle.ts`, `applyStudentStatus` and `EnrollmentService.enroll` | `server/src/tests/work-packages/wp03/student-subsystem-remediation.test.ts`, `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | invalid transition/enrollment returns 409; graduation completes coupled rows atomically |
| Suspending a student suspends all active enrollments and defers only the active semesters captured by that suspension; only the dedicated resume command restores those exact semesters, and ordinary status/general enrollment paths cannot bypass it | application transaction + database mapping | `student_suspension_batches`, `student_suspension_semesters`, `EnrollmentService.suspend/resume` | `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts`, `server/src/tests/work-packages/wp05/enrollment-subsystem-remediation.test.ts` | incompatible state, full class, or missing exact restoration mapping returns 409 and rolls back; unrelated deferred history remains deferred |
| A student occupies at most one seat per class and semester, while sequential completed terms remain legal | database + application | active-seat unique index plus `server/src/core/academic/class-admission.ts` | `server/src/tests/work-packages/wp03/student-subsystem-remediation.test.ts`, `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | duplicate active admission returns 409; completed-term repeat remains accepted |
| Class capacity counts distinct students from seat-consuming enrollments | application | `server/src/core/academic/class-capacity.ts` at every WP-03 admission path | `server/src/tests/work-packages/wp03/visitors.test.ts`, `server/src/tests/work-packages/wp05/enrollment-subsystem-remediation.test.ts` | full class returns 409 before any partial admission write |
| Visitor conversion preserves the visitor branch, uses the selected class fee, and commits visitor/student/enrollment/invoice/payment state atomically | application transaction | conversion preflight and transaction in `server/src/routes/visitors.routes.ts` plus `EnrollmentService.enroll` | `server/src/tests/work-packages/wp03/visitors.test.ts`, `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | branch/fee/eligibility mismatch returns 400/409; late failure leaves no partial records |
| A journey is append-only chronology, not a second lifecycle or financial authority | application | `server/src/core/journey/journey-engine.ts`, note-only `POST /journey/events`, canonical-state overlays | `server/src/tests/work-packages/wp03/timeline-separation.test.ts`, `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | manual lifecycle events return 400; current state comes from owning tables |
| Student academic access never widens class/own object scope through roster, summary, search or CSV export | application | `resolveStudentReadScope`, `studentAuthorityFilter`, and shared student filter builder | `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | unrelated students are absent from collections and exports |
| Student and journey financial fields require Payment.View correlated to the student branch except on authorized self-service | API + UI | finance-aware maps in student/journey routes, assignment-correlated branch checks and exact permission UI controls | `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts`, `server/src/tests/work-packages/wp02/identity-access.attack.test.ts` | tuition/payment fields, nested money payloads and financial events are omitted; finance authority from another branch cannot unredact them |
| A student debt-hold override must come from an Owner, General Manager or Finance Manager assignment that reaches the student's branch | application | `checkAcademicHold` and `canAccessBranchForRequirement` in `server/src/routes/students.routes.ts` | `server/src/tests/work-packages/wp03/students-admissions.attack.test.ts` | an unrelated-branch finance assignment cannot lend an override to an academic assignment |
| One canonical placement profile exists per program-version/branch scope, with at most one global fallback | database + application | profile unique indexes in `server/src/db/schema.sql`; hierarchy in `server/src/core/placement/policy-engine.ts` | `server/src/tests/work-packages/wp04/profile-policy.integration.test.ts` | duplicate storage aborts; a policy outside candidate → program branch → global hierarchy resolves as configuration error |
| A placement blueprint stores structurally valid component, scoring, timing, retake and decision facts; required/optional policies cannot start without valid components | API + application + database | strict writer in `server/src/routes/academic.routes.ts`; validation in `server/src/core/placement/policy-engine.ts` and `server/src/core/placement/store.ts`; schema checks | `server/src/tests/work-packages/wp04/profile-policy.integration.test.ts`, `server/src/tests/work-packages/wp04/attack.test.ts` | malformed writes return 400; corrupted stored policy returns 409 without an attempt |
| A visitor has at most one open placement attempt and monotonically unique attempt numbers | database | `uq_placement_open_attempt` and `UNIQUE(visitor_id, attempt_number)` in `server/src/db/schema.sql` | `server/src/tests/work-packages/wp04/attempt-lifecycle.integration.test.ts`, `server/src/tests/work-packages/wp04/attack.test.ts` | duplicate/concurrent start returns 409 and no partial result rows persist |
| Placement attempts preserve immutable policy, content, answer keys, rubric, billing terms and prior-attempt facts while operational projections never expose answer keys | application + database | snapshot creation/redaction in `server/src/routes/placement-attempt.routes.ts` and `server/src/core/placement/store.ts`; snapshot-correlation triggers | `server/src/tests/work-packages/wp04/attempt-lifecycle.integration.test.ts`, `server/src/tests/work-packages/wp04/test-bank-security.integration.test.ts` | mutable authoring changes cannot alter the sitting; secret fields are omitted; forged response/result rows abort |
| Placement test/rubric assets remain branch- and kind-correlated, and media bytes match their MIME and remain inside the resolved media directory | API + database + filesystem | `server/src/routes/placement-test-bank.routes.ts`; rubric scope/kind triggers in `server/src/db/schema.sql` | `server/src/tests/work-packages/wp04/test-bank-security.integration.test.ts`, `server/src/tests/work-packages/wp04/attack.test.ts` | foreign/mismatched assets return 400/403/409; spoofed files return 415; lexical or symlink escapes return 409 |
| Overall attempt expiry is wall-clock authority; pause may freeze component timers but never extends `expires_at` | application transaction | `server/src/core/placement/timing-engine.ts`; scoped maintenance in `server/src/routes/placement-attempt.routes.ts` | `server/src/tests/work-packages/wp04/attempt-lifecycle.integration.test.ts`, `server/src/tests/work-packages/wp04/attack.test.ts` | late submissions/expired attempts return 409 and the visitor pointer is reset atomically |
| Placement percentage, weighted score, recommendation and pass/fail outcome derive only from snapshotted policy and persisted server scores | application + database | `server/src/core/placement/scoring-engine.ts`, `server/src/core/placement/decision-engine.ts`, result score checks in `server/src/db/schema.sql` | `server/src/tests/work-packages/wp04/scoring-decision-correction.integration.test.ts` | aliases/out-of-range scores return 400; unmet minimum/threshold facts produce a persisted failed outcome |
| A post-completion correction recomputes result, outcome, recommendation and visitor projection atomically while preserving override and provenance | application transaction | correction boundary in `server/src/routes/placement-attempt.routes.ts` | `server/src/tests/work-packages/wp04/scoring-decision-correction.integration.test.ts` | unauthorized/invalid correction returns 403/400/409; no partial projection drift is committed |
| A billable placement sitting creates at most one payment and one linked income transaction from start-time billing facts; a free sitting creates neither | application transaction + database | `server/src/core/placement/placement-policy.ts`, completion transaction in `server/src/routes/placement-attempt.routes.ts`, `server/src/utils/income.ts` | `server/src/tests/work-packages/wp04/retake-billing.integration.test.ts` | duplicate completion returns 409; transaction rollback prevents orphan payment/income; failed sittings remain auditable and billable |
| Placement-required conversion and every class-enrollment writer require the latest authoritative completed pass or an established waiver/not-required result | application | `server/src/core/placement/placement-policy.ts`, `server/src/core/placement/enrollment-gate.ts`, shared enrollment consumers | `server/src/tests/work-packages/wp04/enrollment-gates.integration.test.ts` | unlinked, incomplete, failed, foreign-policy or missing-outcome candidates are refused before enrollment writes |
| Academic program, version, level, subject, offering and configured-class identifiers remain one branch-correlated ownership graph; a course-offering level has the exact selected version, while an unversioned staging level requires an explicit compatible assignment command | database + application | correlation/CHECK/prerequisite triggers in `server/src/db/schema.sql`; `server/src/routes/academic.routes.ts`, `server/src/routes/offerings.routes.ts`, `server/src/routes/classes.routes.ts` | `server/src/tests/work-packages/wp05/attack.test.ts`, `server/src/tests/work-packages/wp05/academic-prerequisite-integrity.test.ts`, `server/src/tests/work-packages/wp05/course-offering-version-compatibility.test.ts` | malformed/coerced/cross-graph input returns 400/409 or aborts; no partial catalog copy persists |
| Class coarse status is the exact projection of lifecycle stage, and configured curriculum identity is either a complete `(program_id, level_id)` pair or absent | database + application | class integrity triggers plus `server/src/core/academic/lifecycle-engine.ts` | `server/src/tests/work-packages/wp05/attack.test.ts`, `server/src/tests/work-packages/wp05/academic-lifecycle.test.ts` | projection or partial-identity write aborts; transitions use the lifecycle authority |
| Offering capacity is derived from linked class capacities, and every linked class matches offering program/version/level/branch/term identity | database + application | offering/class correlation triggers; level-version assignment command; derived query in `server/src/routes/offerings.routes.ts` | `server/src/tests/work-packages/wp05/attack.test.ts`, `server/src/tests/work-packages/wp05/course-offering-version-compatibility.test.ts` | incompatible link/update returns 400/409 or aborts; capacity read equals the linked-class sum; the valid offering path previews, drafts and publishes a correlated class |
| A transfer names one active source enrollment, closes only its exact active semester projection, and commits lifecycle/events/request history atomically; an active freeze is unique, enrollment-correlated and closes atomically with exit from frozen state | database + application transaction | `server/src/core/academic/enrollment-service.ts`, `server/src/routes/enrollment.routes.ts`, freeze/transfer constraints and triggers | `server/src/tests/work-packages/wp05/attack.test.ts`, `server/src/tests/work-packages/wp05/transfer-freeze-waitlist-engines.test.ts`, `server/src/tests/work-packages/wp05/enrollment-subsystem-remediation.test.ts` | stale/CAS/correlation failure returns 409 or aborts and rolls back every lifecycle/projection/history write |
| Waitlist offer consumes a real positive-capacity opening in FIFO order, and conversion uses EnrollmentService as the sole enrollment/semester/roster writer | application transaction + database | `server/src/routes/waitlist.routes.ts`; active-entry uniqueness/scope/position constraints in `server/src/db/schema.sql` | `server/src/tests/work-packages/wp05/attack.test.ts`, `server/src/tests/work-packages/wp05/transfer-freeze-waitlist-engines.test.ts` | full/out-of-order/duplicate processing returns 409 with no queue or enrollment drift |
| Teacher performance changes only with an evaluation row, and every route/storage request to make a teacher inactive refuses while nonterminal direct or ongoing primary/assistant teaching work exists | database + application transaction | evaluation transaction and active-work guard in `server/src/routes/teachers.routes.ts`; teacher/evaluation constraints and trigger | `server/src/tests/work-packages/wp05/attack.test.ts`, `server/src/tests/work-packages/wp05/teacher-evaluation-integrity.test.ts`, `server/src/tests/work-packages/wp05/teacher-update-validation.test.ts` | generic score writes return 400; invalid evaluation aborts; active-work deactivation returns 409 |
| A teaching assignment correlates class, teacher, branch and optional class session; dates include the linked session, rate is whole non-negative AFN, class-scoped identity is unique and no class has more than three distinct primary/assistant skills | database + application | assignment CHECKs/indexes/triggers in `server/src/db/schema.sql`; `server/src/routes/skills.routes.ts` | `server/src/tests/work-packages/wp05/attack.test.ts`, `server/src/tests/work-packages/wp05/teacher-assignment-engine.test.ts` | malformed/cross-scope/duplicate/fourth-skill write returns 400/409 or aborts |
| Session attendance facts exist in exactly one table — never a mirror | application (writers) + schema (attendance has no session column) | roster mark commands write `rosters` only; the day-level router writes `attendance` only | `server/src/tests/work-packages/wp06/attendance-authority.test.ts` | any second copy would drift on the single-mark PATCH path | 
| Every student-attendance read surface consumes the one union authority | application | `server/src/core/academic/attendance-query.ts` consumed by report metrics, impact, list and summary | `server/src/tests/work-packages/wp06/attendance-authority.test.ts` | a divergent consumer would disagree with the attendance report | 
| Teacher attendance never pollutes student attendance surfaces or metrics | application | union expressions exclude `target_type='teacher'` | `server/src/tests/work-packages/wp06/attendance-authority.test.ts` | inflated student attendance counts | 
| A session with recorded marks cannot be deleted | application | DELETE /sessions/:id refuses when any roster mark exists | `server/src/tests/work-packages/wp06/attendance-authority.test.ts` | 409; attendance history is never cascade-deleted | 
| A completed session's date and time are immutable | application | PUT /sessions/:id refuses date/time changes on completed sessions | `server/src/tests/work-packages/wp06/attendance-authority.test.ts` | 409; history cannot move between analytics periods | 
| Session dates and times are real calendar values | application | `server/src/utils/isoDate.ts` (`assertOptionalIsoDate`, `assertTimeOfDay`, `assertTimeRange`) at every session writer | `server/src/tests/work-packages/wp06/session-input-guards.test.ts` | 400; no malformed date/time is stored | 
| Certificate revocation is a state transition, never a deletion | application + schema CHECK on `certificates.status` | correction-down updates `status='revoked'` with `revoked_at`/`revoked_by`; no delete path exists | `server/src/tests/work-packages/wp06/exam-correction-financial-invariant.test.ts`, `server/src/tests/work-packages/wp06/exam-certificate-fee-integrity.test.ts` | the issuance fact is retained as revoked history | 
| `status` is the scored marker; a scored result (any score, including 0) cannot be re-entered | application + schema CHECK on `exam_results.status`/`score` | score-entry gate reads `status !== 'pending'` | `server/src/tests/work-packages/wp06/exam-scope-and-storage.test.ts` | 409; only the privileged correction tool changes a recorded score | 
| Exam creation honors assignment scope, never identity branch | application | `resolveBranchScope` on POST /exams | `server/src/tests/work-packages/wp06/exam-scope-and-storage.test.ts` | 403; an unauthorized branch cannot receive an exam | 
| Grade scores are non-negative and bounded by the assessment max score | application + schema CHECKs | grades writer and `student_grades`/`class_assessments` CHECKs | `server/src/tests/work-packages/wp06/gradebook-bounds.test.ts` | 400 / constraint violation | 
| Issued output and revoked history are reported separately | application | certificates count surfaces filter `status='issued'` | `server/src/tests/work-packages/wp06/exam-scope-and-storage.test.ts` | revoked certificates never inflate issued counts | 
