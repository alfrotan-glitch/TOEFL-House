# Protocol Conflict Register

Requirement 4 of the governing directive: *where the repository conflicts with the
protocol, the protocol governs and the repository must be corrected.*

This register lists every conflict found between the registered
`docs/MASTER_ENGINEERING_PROTOCOL.md` and the repository. Destructive corrections require
CHECKPOINT (§65) before execution; the owner granted that approval on 2026-08-20
(Decision D-18), so rows may now move from pending to corrected. Each row states its
current status and the evidence for it.

| # | Protocol | Repository state (evidence) | Verdict | Class (§92) | CHECKPOINT |
|---|---|---|---|---|---|
| C-1 | §12, §15, §95, §96 — legacy migrations are not authoritative and must be **removed**; ONE canonical schema; clean rebuild must not depend on historical migrations | Was: 78 forward-only migration files plus a replay-based preflight, and a scaffold column in the schema existing only so migration 003 could run | **CORRECTED** — the chain, the runner and the scaffold are gone. `server/src/db/schema.sql` is the sole authority (111 tables · 228 indexes · 64 triggers), proven object-for-object identical to the fully-migrated shape (403/403), and `npm run preflight:schema` proves it stands alone, is sound, is idempotent and has no rival | REMOVAL + ARCHITECTURE CHANGE | Approved (D-18) |
| C-2 | §14, §104 step 17 — inventory then **remove the legacy active test suite**, rebuild from the domain model | **INVENTORY DONE** (`docs/legacy-test-inventory.md`, 151 files / 1877 declared cases mapped to Work Packages with their asserted behaviour). The suite is deliberately retained as the oracle proving the migration collapse was behaviour-preserving, and is retired per Work Package as each one's replacement lands | **IN PROGRESS** | REMOVAL | Approved (D-18) |
| C-3 | §84, §85 — remove obsolete documentation; do not preserve contradictory historical documents in the active project | Was: 38 dated point-in-time audit narratives in `docs/` plus 22 in an audit-history folder, plus two stale root handoff documents that cited deleted files and a test count off by a factor of five | **CORRECTED** — all removed. The active documentation set is the protocol, the registries, the operations runbook, the legacy test inventory and the WP-07 certification record. Source comments that cited the deleted audits were rewritten to state current intent (§4) | REMOVAL | Approved (D-18) |
| C-4 | §4 — active source contains no historical narrative; §3 — no compatibility layers or legacy naming | **The recorded figures were wrong and were replaced by a measured count.** The register said 57 "legacy" occurrences, 16 commented-out code lines and 1 "TEMP"; those came from raw greps over whole files. Measured over comment text only, active source held **107 banned-phrase comment lines in 53 files**. There were **zero** TODO/FIXME/HACK/XXX/"workaround"/"temporary fix" comments — already clean — the "1 TEMP" was a substring of "template", and the 16 commented-out code lines could not be reproduced at all. The measurement also exposed what a comment count hides: legacy identifiers and live compatibility paths in executing code | **CORRECTED** — every banned phrase is gone from active source, rewritten as present-tense intent rather than deleted, because §4 keeps rationale that explains the current architecture. Removed or renamed in code: `ensureBranchColumns` (proven no-op, and divergent from canonical if it had ever fired), `LEGACY_MODEL_ALIASES` (unreachable — `teachers.salary_type` CHECKs the canonical five and the API rejects the aliases with 400; the one unconstrained column, `teacher_compensation_history.salary_type`, now carries the same CHECK, which is what made deletion safe), `LegacyClassStatus`/`deriveLegacyClassStatus` → `CoarseClassStatus`/`deriveCoarseClassStatus`, and the legacy-named config audit script → `server/scripts/config-data-integrity-audit.mjs`. Two comments were actively false and would have misled a reader into deleting live code: the attendance router was labelled "@deprecated Legacy … kept for backward compatibility only" while serving the frontend and backing two report-catalog metrics, and `server/src/db/financeCategoryCatalog.ts` documented the deleted migration runner as one of its two callers. `npm run audit:cleanliness` (gate 21) fails the build on any of 10 banned phrases in a comment in `server/src` or `src`. It reads comments only, skipping string literals, and excludes "historical" and "no longer" (legitimate domain words) and the verb "be used to" — all three measured as false positives before the list was fixed | REFACTOR | No |
| C-5 | §83 — avoid uncontrolled logging; logs structured, useful, non-sensitive | **The recorded figure was wrong.** This register said "268 `console.log` in runtime source"; the actual runtime count was **53** (25 log, 16 error, 12 warn) across 14 server modules, plus 1 in the frontend. The 268 conflated CLI tooling — `scripts/` holds 305 console calls, where console output IS the interface, not a defect | **CORRECTED** — `server/src/core/observability/logger.ts` gives every line a level, a source and a shape: JSON per line in production, compact text in development, silent below `error` under test. 44 calls migrated; the seeder is named as a CLI. Credential-shaped context keys are redacted before output. `npm run audit:logging` fails the build on a raw `console.*` in service code or if redaction is removed — both provoked and observed | REFACTOR | No |
| C-6 | §2 LAW 1 — exactly one authority per concept | **Two protocol documents existed** — the superseded v2 protocol alongside this one | **CORRECTED IN THIS PASS** — v2 removed; `npm run audit:protocol` now fails if it reappears | REMOVAL | No |
| C-7 | §11, §86 — no compatibility layers without a real external requirement | `saving_accounts` was marked `LEGACY_COMPAT_ONLY`: created on every branch, deleted with every branch, seeded on every install — and **never read**. Its `balance` was only ever inserted as 0 and never updated, so it held no financial fact; the real authority is `finance_accounts.saving_balance`, which is read and updated by `server/src/utils/financeAccounts.ts` | **CORRECTED** — table and all four write sites removed after the §43 checklist: no read anywhere, no foreign key, trigger, index or view referencing it, zero test dependencies, no script or configuration use. Live proof before removal showed a seeded database holding `{branch_id:'1', balance:0}` beside the real `finance_accounts` row. Removal reduced the schema to 110 tables; it is now 111 after D-58 added the distinct, required `notification_read_receipts` authority (the deleted shadow table did not return) | REMOVAL | Approved (D-18) |
| C-8 | §22, §2 LAW 1, §47 — one authority and one vocabulary per concept | **RESOLVED.** `users.role` no longer exists. Two defects were reproduced before removal (revocation was a no-op — deleting every assignment left all 74 permissions, re-granted from the column; and `users.role='owner'` synthesized an organization-scoped owner). The column, its CHECK, its index, `LEGACY_ROLE_MAP`'s authorization use, the `syncLegacyUserRoles`/`syncPrimaryUserRole` provisioning pair, the JWT `role` claim and the `canAccessBranch` home-branch fallback are all gone. Roles, permissions and scope derive from `user_roles` alone; the API's reported role is derived from the primary assignment. Proven at four levels — schema (column unwritable/unreadable, no index), code (both typechecks, `assignPrimaryRole` the only grant path), tests (153 files / 2334 passing, `rbac-single-authority.test.ts` asserts the column's absence), and live adversarial run (200→403 on revocation with an unchanged valid token, permissions 101→0, escalation attempt rejected by the schema, access restored only by re-assignment) | **CORRECTED** | ARCHITECTURE CHANGE | Approved (D-14) |
| C-9 | §31 — printing is first-class where operationally required | Was: six components each calling `window.open` and writing their own `<style>`. No `@page` (paper size and margins left to the browser), `text-align:left` hard-coded so a Persian document printed left-aligned, no repeated table headers so page two of a table was unlabelled, no page numbers so a dropped sheet was undetectable, and no break control so totals split from their tables | **CORRECTED** — `src/design-system/print.ts` owns paper setup and is asserted by 26 tests because a popup cannot be reviewed by eye. All six generators now build through it and the audit's named exemption list is removed rather than left empty. The three bespoke documents needed the shell to learn that paper is not always A4: a registration receipt is an 80mm till roll (A4 report margins paginate a short receipt onto a second sheet) and an ID card is a card, so `paper: 'a4' | 'receipt80' | 'card'` is chosen by name — a caller selects a paper, it cannot invent a page rule. The salary slip was the one genuinely broken: it printed by scraping `innerHTML` out of the modal, carrying Tailwind class names into a window with no Tailwind (every rule silently did nothing) and hard-coding `direction: rtl` on a document whose labels read "Base Amount". It is now built from the payment data. The student ID card had no `@page` at all | ARCHITECTURE CHANGE | No |
| C-10 | §24 — a coherent design system must exist before multiplying UI implementations; §26 — RTL is a first-class layout mode | Design TOKENS already existed (`@theme` in `src/index.css`); what was missing was anything built on them and any direction architecture. One concept had four spellings (a form label appeared as `block text-slate-600 mb-1 font-medium` 39×, `…font-medium` 30×, `…font-medium mb-1` 22×, `…font-semibold mb-1` 17×), and 236 physical direction utilities across 53 files plus 37 hard-coded `dir="ltr"` attributes made RTL a per-screen retrofit | **CORRECTED** — `src/design-system/` adds the component layer (typography, layout, surface, control, button, badge, table recipes) on the existing tokens; all 238 physical utilities are now logical (`ms-`/`pe-`/`text-start`/`start-`), direction is owned solely by `DirectionProvider` on `<html>`, and `npm run audit:design-system` fails the build if either property regresses. Both failure modes were provoked and observed | ARCHITECTURE CHANGE | No |
| C-11 | §92 — historical migration files are not required merely because they existed | Was: version-keyed hooks in the migration runner (for the visitor-stage rebuild and the finance-category legacy mapping) servicing only historical migrations | **CORRECTED** — removed with C-1. The finance catalog seed it performed is reached on every install through `ensureOrganizationHierarchy`, so no behaviour was lost | REMOVAL | Approved (D-18) |

## Conflicts deliberately NOT raised

| Item | Why it is not a conflict |
|---|---|
| `git` history contains legacy commits | §5 explicitly forbids destroying history; only the *active* codebase must be clean |
| The WP-07 certification record references the superseded v2 protocol | It is an accurate historical record of a certification performed under v2. It is annotated as requiring re-certification under this protocol rather than rewritten, because rewriting it would be a false claim (§103) |
| `teacher_salary_ledger` / `employee_salary_ledger` table names contain "salary" | Domain-correct names for payroll ledgers, not legacy residue (§47) |
