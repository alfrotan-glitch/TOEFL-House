# Decision Log

Final architectural decisions only. This is history — it stays here and **out of source
comments**, per the source-cleanliness rule in the protocol.

| Date | WP | Decision | Alternatives considered | Evidence class | Reversible? | Approved by |
|---|---|---|---|---|---|---|
| 2026-08-20 | WP-07 | Adopt a two-level canonical finance taxonomy (10 categories / 45 subcategories) with channels below subcategories | keep the flat per-branch `purpose` model; three-level taxonomy | PROVEN | Yes (migration) | Owner |
| 2026-08-20 | WP-07 | Facebook is modelled as a **channel** of Digital Advertising, never an accounting category | a `Facebook Advertising` subcategory | PROVEN | Yes | Owner |
| 2026-08-20 | WP-07 | `financial_transactions.finance_category_id` (FK) replaces the copied category string as the accounting authority | keep the string and a lookup table | PROVEN | No (forward-only migration) | Owner |
| 2026-08-20 | WP-07 | **Clean-slate exception**: delete the legacy budget model outright rather than preserving compatibility. Ground: pre-operational system, no production financial data. Scope: WP-07 category/budget model only. **Spent — does not carry forward.** | extend-and-map (migrations 077/078, superseded) | PROVEN | No | Owner |
| 2026-08-20 | WP-07 | Taxonomy and budget are separate concerns: a fresh branch is provisioned with **2** payroll envelopes, not one line per subcategory | seed 45 zero-value lines per branch | PROVEN | Yes | Owner |
| 2026-08-20 | WP-07/08 | Payroll resolves its envelope through `budget_lines.payroll_target`, a modelled business relationship | reuse a name/purpose string; single shared Salaries & Wages line; caller-supplied id | PROVEN | Yes | Owner (option E-1) |
| 2026-08-20 | WP-08 | Teacher `advance` removed — capped at the period's remaining due, so it was a partial payment of earned salary. Employee `advance` retained and classified as a Non-Expense Cash Movement. | reclassify both; reclassify neither | PROVEN | Yes | Owner (option 11-C) |
| 2026-08-20 | WP-07 | Reserve is **not** part of the expense taxonomy and has no budget line; the six-month reserve rule stays treasury/BOS policy | model Reserve as an expense subcategory | PROVEN | Yes | Owner |
| 2026-08-20 | — | Adopt Engineering Protocol v2: Work Package scope, CHECKPOINT before irreversible actions, mandatory INDEPENDENT REVIEW, three certification statuses | keep Protocol v1 (58 phases, whole system, one pass) | SUPPORTED | Yes | Owner |
| 2026-08-20 | — | Registries are executable: `npm run audit:registries` runs in the release gate, so a stale registry fails the build | keep registries as prose | PROVEN | Yes | Agent, under protocol §5 |
| 2026-08-20 | — | **D-11 Currency:** AFN is the sole currency. No secondary currency, no FX rate, no multi-currency columns or conversion logic anywhere | support a second currency; build FX-ready abstractions now | DECISION (owner) | Yes | Owner (Q1) |
| 2026-08-20 | WP-07 | **D-12 Money representation:** no floating-point money. One canonical representation, enforced system-wide by the money authority and by database constraints | keep REAL with 2dp triggers; decimal strings | DECISION (owner) | No | Owner (Q2) |
| 2026-08-20 | WP-11 | **D-13 Reporting scope:** every §30 report category is required end-to-end — daily, weekly, monthly, quarterly, annual, financial, academic, student, visitor, enrollment, attendance, teacher, payroll, management, operational, audit. A report is not omitted because the legacy system lacked it | implement only the legacy report subset | DECISION (owner) | Yes | Owner (Q3) |
| 2026-08-20 | WP-02 | **D-14 Multi-position RBAC:** a user may hold multiple positions/roles. Exactly ONE canonical RBAC authority; conflicting permissions resolve through an explicit deterministic authorization policy. No duplicated role/permission authority | single-role-per-user; keep `users.role` alongside `user_roles` | DECISION (owner) | No | Owner (Q4) |
| 2026-08-20 | — | **D-15 Bilingual UI:** English + Persian/Dari with first-class LTR/RTL architecture across components, layouts, forms, tables, reports, printing, dashboards and mixed-script content. RTL may not be scattered overrides | keep English-LTR chrome (the pre-existing repository position) | DECISION (owner) | No | Owner (Q5) |
| 2026-08-20 | WP-10 | **D-16 Book issuance:** implement the complete book issuance/lending domain as a distinct concern from book sales | treat `book_sales` as the whole Books domain | DECISION (owner) | Yes | Owner |
| 2026-08-20 | WP-06 | **D-17 Diplomas:** diploma/certificate is a complete academic output domain; its canonical model is derived from requirements and architecture rather than inherited from the existing `certificates` table | assume `certificates` already satisfies it | DECISION (owner) | Yes | Owner |
| 2026-08-20 | — | **D-18 Destructive reconstruction:** remove all legacy migrations and migration machinery; rebuild the canonical schema from zero; no replacement migration mechanism unless the final architecture requires one; remove the legacy test suite and rebuild the test architecture; remove obsolete artifacts per the Deletion Protocol. Git preserves recoverability; the active project preserves nothing "just in case" | incremental compatibility engineering | DECISION (owner) | No (Git-recoverable at `c809d0d`) | Owner (CHECKPOINT APPROVED) |
| 2026-08-20 | — | **D-19 Collapse before redesign:** the 78-file migration chain was collapsed into the canonical `schema.sql` at its *existing* shape, proven object-for-object equivalent, before any domain redesign. Redesign then proceeds by editing the canonical schema per Work Package | collapse and redesign in one step | PROVEN (403/403 objects identical; 151 files / 2318 tests green) | Yes | Agent, under §12/§104 |
| 2026-08-20 | WP-02 | **D-20 One role vocabulary:** authorization guards name canonical `roles.code` values. `RoleCode` is derived from `ROLE_CODES`, so the compiler rejects an unknown role instead of the guard silently matching nobody. Workflow step roles use the same vocabulary. `LEGACY_ROLE_MAP` survives only as a one-way translation at the user-provisioning boundary | keep the legacy vocabulary and translate at every guard; maintain both vocabularies | PROVEN (265 literals converted, 153 test files green, gate 18/18) | Yes | Agent, under D-14 |
| 2026-08-20 | WP-02 | **D-21 Remove `users.role`:** a principal's positions live in `user_roles` only. The denormalized column, its index, the JWT `role` claim and the legacy provisioning pair are removed; `assignPrimaryRole` is the single grant path and the API derives the reported role from the primary assignment | keep the column as a denormalized display attribute | PROVEN (schema, code, 2334 tests, live revocation 200→403) | No | Owner (directive) |
| 2026-08-20 | WP-07 | **D-22 Canonical money representation:** AFN is stored as an INTEGER number of whole afghani. Established from three independent observations, not preference: display was already 0dp everywhere (`formatAFN`), every money-producing computation already rounded to a whole afghani (discounts, payroll, BOS allowance, book pricing), and nothing anywhere refers to the pul sub-unit. Rounding is half away from zero so a contra entry exactly reverses its original | keep REAL with 2dp triggers; integer minor units (pul) | PROVEN (53 columns converted, 2333 tests, gate 18/18) | No | Owner (Q2) |
| 2026-08-20 | WP-07 | **D-23 Reject, do not round, operator input:** `assertMoney` refuses a fractional amount; `assertComputedMoney` settles a value the system derived (a percentage, a multiplier, a share). Silently turning a typed 24000.50 into 24001 stores a figure nobody entered. This extends the policy the fee and invoice writers had already adopted ("reject rather than silently round") to every money boundary, so there is one rule instead of two | round everywhere; reject everywhere including computed values | PROVEN (2333 tests) | Yes | Agent, under D-12 and LAW 6 |
| 2026-08-20 | — | **D-24 Direction is inherited, never pinned:** `DirectionProvider` sets `lang`/`dir` on `<html>` once; components use logical properties and never ask which direction they are in. `dir="auto"` remains allowed for user-entered text of unknown script, and printed documents set their own direction because they are separate documents | keep `dir` on each view root; add an RTL stylesheet | PROVEN (238 utilities converted, 37 pinned attributes removed, audit provoked) | Yes | Agent, under D-15 |
| 2026-08-20 | — | **D-25 The design system is enforced, not offered:** `npm run audit:design-system` runs in the release gate. An available-but-optional design system gets bypassed; a mechanical one cannot be | rely on review to catch drift | PROVEN (both failure modes provoked and observed) | Yes | Agent, under §24/§85 |
| 2026-08-20 | WP-11 | **D-26 Reports are declared, not written:** a report names metric ids, a period granularity and a permission; the engine is the only thing that turns that into numbers. Sixteen required categories built as bespoke endpoints would be sixteen chances for one figure to be computed two ways | one hand-written endpoint per report | PROVEN (20 tests; same metric compared across two reports) | Yes | Agent, under D-13/§18 |
| 2026-08-20 | — | **D-27 Weekly and quarterly join the period authority:** `periodBoundaries` now resolves today/week/quarter/month/year in the Shamsi calendar. The dashboard declares the three-period subset it presents, so widening the authority does not silently widen every consumer's contract | let reports compute their own week/quarter | PROVEN (compiler caught the dashboard coupling) | Yes | Agent, under §19 |
| 2026-08-20 | WP-11 | **D-28 One calendar for every report:** `/reports/overview` resolved its own **Gregorian** month/quarter/year while Finance, payroll and the dashboard resolved **Shamsi** ones. Proven to disagree on 5/5 sampled days. It now delegates to `periodBoundaries`, and a historical period is named by its Shamsi key (`?key=1405-03`) resolved by `periodBoundariesForKey` | keep Gregorian historical params alongside Shamsi named periods | PROVEN (divergence measured, then removed; overview and engine assert the same span) | No (contract change) | Agent, under §19/§77 |
| 2026-08-20 | WP-11 | **D-29 Two report assemblies, one classification authority:** `/reports/overview` and the report engine assemble their queries differently (one gender-split CASE query vs one query per metric) but both call `ledger-classification`. That is one authority with two consumers, which LAW 1 permits — verified by reconciliation rather than assumed | force both onto a single query shape | PROVEN (figures compared against an independent ledger sum; divergence provoked and caught) | Yes | Agent, under §77 |
| 2026-08-20 | — | **D-30 One print shell, bespoke content allowed:** the print authority owns properties of PAPER (size, margins, repeated headers, page numbers, break control, direction); a document may supply its own `extraCss` for bespoke design. Building the document is separated from opening the window so the markup can be asserted — a popup cannot be reviewed by eye | one stylesheet for every printed artifact; or leave each generator standalone | PROVEN (16 tests; audit provoked) | Yes | Agent, under §31 |
| 2026-08-20 | — | **D-31 One logger, and CLI output is not logging:** service modules log through `createLogger`; `scripts/` and the seeder keep console output because it is their operator interface. Production emits JSON per line, tests are silent below `error`. Context is redacted for credential-shaped keys | route every console call through the logger, including CLI tools | PROVEN (9 tests; both audit checks provoked) | Yes | Agent, under §83 |
| 2026-08-20 | WP-07 | **D-32 Remove `saving_accounts`:** a permanently-zero shadow of `finance_accounts.saving_balance`, written and deleted in lockstep with branches but never read. Removed rather than kept "in case", per the Deletion Protocol — a second savings table that no code consults is a trap for the next reader, not a safety net | keep it as a compatibility surface | PROVEN (§43 checklist; live seeded-database evidence; 2387 tests) | No (schema) | Owner (D-18) |
| 2026-08-20 | WP-02 | **D-33 One role vocabulary on each side of the wire:** `USER_ROLE_CODES` is declared once in `src/types.ts`; `UserRole`, the `USER_ROLES` Set, `ROLE_LABELS` and `ASSIGNABLE_ROLES` all derive from it, and `ROLE_LABELS` is typed `Record<UserRole, string>` so the compiler rejects a key that is not a role. Lists that intentionally withhold a role (`NOT_ASSIGNABLE` on the server, `NOT_OFFERED_AS_ACCOUNT_TYPE` in the UI) are expressed as exclusions from the vocabulary, so a role added to the catalog is offered by default and every exception is stated | keep the parallel lists and compare them in review | PROVEN (`role-vocabulary.test.ts`, 11 tests; stray key, dropped role and retyped list each provoked and observed; `tsc` independently rejects the stray key with TS2353) | Yes | Agent, under LAW 1 and D-20 |
| 2026-08-20 | WP-02 | **D-34 The server resolves effective access; the client reads it:** `effectivePermissionCodes` and `effectiveTabAccess` in `rbac-service.ts` apply the global-owner bypass once, and `/auth/login`, `/auth/me` and `/security/me/permissions` all serialize their result. The reported permission set is what the server will actually honour, not the stored grant — the catalog withholds four destructive codes from the owner's stored row for audit reasons while `requirePermission()` bypasses the owner anyway, so reporting the stored set would have hidden controls the API accepts | let each endpoint apply the owner rule itself; expose the stored grant and let the client compensate | PROVEN (`tab-access-authority.test.ts`, 14 tests; owner-bypass removal provoked and observed) | Yes | Agent, under LAW 1 and §22 |
| 2026-08-20 | WP-02 | **D-35 The sidebar carries no authorization vocabulary:** the 18 per-item `roles: [...]` lists and the `TAB_ACCESS` map compiled from them are removed, along with the client's `role === 'owner'` short-circuits. `canAccessTab(tab, tabAccess)` reads the server's answer and nothing else; absence is denial. Navigation config now declares structure and order only | keep the role lists as a client-side fallback for pre-RBAC payloads | PROVEN (measured against the catalog, the lists disagreed in at least 4 places; every call site found by the compiler; both failure modes provoked) | Yes | Agent, under LAW 1 and §22 |
| 2026-08-20 | WP-02 | **D-36 A routed screen must have a permission that governs it:** `test-bank` was a routed, sidebar-listed screen absent from `TAB_PERMISSION_MAP`, so its access resolved to `undefined` — false — for every user including the owner. `Curriculum.TestBank` is added to the catalog and granted to general_manager and head_of_department, mirroring exactly the roles the test-bank routes already admit via `authorize()`; the mapping is not invented policy but the policy the routes already state | leave the tab unmapped; map it to an unrelated existing code such as `Exam.Create` | PROVEN (defect reproduced by the test before the fix: `expected [ 'test-bank' ] to deeply equal []`) | No (new permission code) | Agent, derived from `placement-test-bank.routes.ts` |
| 2026-08-20 | — | **D-37 Historical narrative is rewritten, not deleted:** §4 keeps a comment that is "genuinely required to understand the current architecture", and most of these were — the numbers in them (2,162 students exported as 2,000; a 27% conversion tile against a true 11%) are what justify the current shape. Each was restated as the property being protected ("counting the page under-reports every channel") instead of the incident ("it used to count the page") | delete the comments; leave them as history | PROVEN (107 rewritten; `audit:cleanliness` PASS over 205 files / 8,083 comment lines) | Yes | Agent, under §4 |
| 2026-08-20 | — | **D-38 The cleanliness gate bans phrases, not prose quality:** 10 unambiguously historical phrases are banned in comments in active source. "historical"/"no longer" are deliberately NOT banned (a completed enrollment IS a historical record) and neither is the verb "be used to" — both were measured as false positives against real code first. Tests and `scripts/` are out of scope: a regression test's purpose is to describe the defect it pins | ban every past-tense construction; rely on review | PROVEN (three failure modes provoked; string literals and the "used to" verb confirmed NOT flagged) | Yes | Agent, under §4/§85 |
| 2026-08-20 | WP-06 | **D-39 One salary-type vocabulary, enforced by every column that stores it:** `teacher_compensation_history.salary_type` had no CHECK while `teachers.salary_type` CHECKed the canonical five, and payroll reads the history column first. Adding the matching CHECK is what makes `LEGACY_MODEL_ALIASES` provably unreachable rather than merely unused, so the alias map is deleted | delete the alias map on the strength of the API-level 400 alone | PROVEN (5 tests; CHECK removal provoked, 2 failures observed) | No (schema) | Agent, under LAW 1 and §13 |
| 2026-08-20 | — | **D-40 The shell owns paper, and there is more than one paper:** `PrintPaper` names `a4`, `receipt80` and `card`; a document selects one and cannot supply its own `@page`. Forcing an 80mm till-roll receipt into A4 report margins is as wrong as printing a report on a till roll, and that mismatch is why the last three documents still had their own `<style>` blocks. With all six migrated, the design-system audit's exemption list is deleted rather than left empty — an empty exemption list is an invitation | keep the exemption list; let bespoke documents write their own `@page` | PROVEN (26 print tests; paper resolution and the Tailwind-scrape regression each provoked and observed failing) | Yes | Agent, under §31 and D-30 |
| 2026-08-20 | WP-07 | **D-41 BOS resolves its period through the calendar authority:** `getTimeBounds` sliced the ISO date into a Gregorian month while everything else resolved a Shamsi month (D-28); the windows disagreed on 8/8 sampled days, and the window ran days into the future while the authority stops at today. Because the withdrawable ceiling subtracts the drawings already taken *using the same window*, a wrong window omits drawings and the ceiling re-opens. An explicit period is now named by its Shamsi key, matching `/reports/overview` | leave BOS on Gregorian months; add a second calendar authority for treasury | PROVEN (money consequence reproduced before the fix: a 50,000 drawing left the ceiling at 200,000 instead of 150,000, and total drawings reached 300,000 against a 200,000 ceiling) | No (contract change: `?period=` is now a Shamsi key) | Agent, under §19/§57 and D-28 |
| 2026-08-20 | WP-07 | **D-42 One profit-distribution authority:** the allowance formula existed twice — once in the endpoint that PUBLISHES the ceiling and once in the endpoint that ENFORCES it — and the two had already drifted, the publisher accepting a caller-supplied period the enforcer ignored. A published limit the enforcing path does not share is not a limit. Both now call `computeProfitDistribution`, which is pure and owns all of the arithmetic | keep two implementations and reconcile them with a test | PROVEN (reconciliation test compares route output against the authority; divergence provoked and observed breaking both the new suite and the pre-existing BOS-1 suite) | Yes | Agent, under LAW 1 and §77 |
| 2026-08-20 | WP-07 | **D-43 Treasury thresholds are declared policy:** the tier table, the six-month reserve multiple and the warning thresholds move to `TREASURY_DEFAULTS` in the configuration catalog, at exactly the values already in force, shaped like the existing `letterGradeBands` band table. Declaring a number is not the same as choosing it — this satisfies LAW 7 and narrows A-10 to the remaining owner question of whether the values are right | invent "better" thresholds; leave them in the route until the owner answers | PROVEN (values pinned by tests; boundary behaviour reproduces the original if/else chain at -50/0/9.99/10/19.99/20/29.99/30/100) | Yes | Agent, under LAW 7 and §51 |
| 2026-08-20 | WP-07 | **D-44 "Last month" is the previous Shamsi month:** the executive dashboard derived its comparison window by stepping one GREGORIAN month back from the period start. Once the period start became a Shamsi month start (D-41) that lands in a window belonging to neither month — from 2026-07-23 it gives 2026-06, overlapping two Shamsi months. `previousMonthKey` in the calendar authority steps in Shamsi month numbers, because Shamsi months are 29/30/31 days depending on the year | subtract 30 days; keep the Gregorian step | PROVEN (year-boundary wrap provoked, observed failing; the previous month is asserted to abut the current one with no gap or overlap) | Yes | Agent, under §19 and D-41 |
| 2026-08-20 | WP-11 | **D-45 Impact reporting uses the one period authority (closes TR-7):** the local Gregorian `periodBounds()` is deleted and `periodBoundariesForKey` resolves the period. These are donor-facing reports stating scholarship funds and donations in AFN, so a period that does not mean what Finance means by the same name produces a figure a donor cannot reconcile. A source scan now fails the build if any route re-declares a period resolver, because a second one can agree today and drift tomorrow | keep the Gregorian resolver and document the difference | PROVEN (a donation on the Shamsi month's first day counted 0 instead of 70,000 before the fix; three failure modes provoked, including re-declaring a local resolver) | No (contract change: the period is now a Shamsi key) | Agent, under LAW 1, §19, §76 |
| 2026-08-20 | — | **D-46 The calendar authority refuses an implausible Shamsi year:** both resolvers accepted `\d{4}-Q[1-4]`, so `'2026-Q1'` was valid to each and meant different centuries — Gregorian 2026-01-01..2026-03-31 versus Shamsi **2647-03-21..2647-06-21**. Swapping resolvers without this guard would have turned the UI's hard-coded `'2026-Q1'` into a year-2647 report with every metric zero and no error. A year more than 100 years from the current Shamsi year is rejected: a sanity bound derived from today, not a business rule, wide enough for any real reporting period and far narrower than the ~621-year calendar offset | trust callers to pass Shamsi keys; hard-code an epoch range | PROVEN (guard removal provoked, 6 failures; the years the system operates in still resolve) | Yes | Agent, under LAW 6 |
| 2026-08-20 | WP-11 | **D-47 The impact report period is chosen by the operator:** the Generate button posted a hard-coded `'2026-Q1'`, so only one period could ever be reported and the key was Gregorian. It now offers the last twelve Shamsi months from `recentJalaliPeriods`, so the key cannot disagree with what the server accepts | keep a fixed period; let the operator type a key | PROVEN (frontend typecheck; the server rejects the previous hard-coded value, which is what forced the change) | Yes | Agent, under LAW 7 |
| 2026-08-20 | — | **D-48 The server package declares what its test suite imports:** the backend CI job installs only `server/`, so a test importing `react` — directly, or through a frontend module under `../src` — resolved locally via the root install and failed in CI. React is now a server devDependency, pinned to the root package's exact versions so the repository resolves ONE React and the suite renders the library production ships. `tsconfig.test.json` maps `react`/`react-dom` at their DECLARATIONS and `vitest.config.ts` aliases them at runtime, because files under `../src` resolve modules by walking up from their own directory and never reach `server/node_modules` | move the React test to a frontend runner (no CI job would execute it); exclude it from the type-check (loses the coverage the config exists to provide) | PROVEN (all four backend CI steps re-run with the root install removed: typecheck, lint, 160 files / 2466 tests, schema preflight — exit 0 each) | Yes | Agent, under §45 |
| 2026-08-20 | — | **D-49 A gate that cannot fail the way CI fails is not a gate:** `release:validate` reported 21/22 green while the backend CI job was red, because the gate runs where a root install satisfies everything. `audit:deps` (gate 22) reads the server import graph and fails when a bare specifier is not declared in `server/package.json`. Reading the graph is deterministic and fast; reinstalling the sandbox to imitate CI on every run is neither | add `server npm run typecheck` to the gate (would still pass locally — it is the environment, not the command, that differs) | PROVEN (the audit reproduces the CI failure exactly, naming react, react-dom and the same three importing files; a fresh undeclared import also provoked) | Yes | Agent, under LAW 9 and §54 |
| 2026-08-20 | — | **D-50 A build-output assertion runs where the build happens:** two assertions in `program-versions-panel-layout.test.ts` read the COMPILED stylesheet from `dist/assets`. The backend CI job installs and runs `server/` alone — it never builds the frontend and cannot, having no vite — so asserting build output there is a category error, and it failed the moment the type-check ahead of it started passing. Those two now skip when the artifact is absent; the guarantee is carried by `release:validate`, which builds before running the suite and is itself a CI job. The block's other assertions read component source and run everywhere | keep them unconditional (the backend job can never satisfy them); delete them (loses a real regression guard) | PROVEN (both halves: they skip cleanly with no `dist`, and still fail when `container-type:inline-size` is removed from the built CSS) | Yes | Agent, under §54 |
| 2026-08-20 | WP-11 | **D-51 The reporting screen is a consumer, never a definition:** `ReportsView` renders the declared catalog and applies only FORMATTING, chosen by the `unit` the server sends. It computes no total, no rate and no aggregate — a screen that adds two metrics to make a third has become a second definition that nothing reconciles, displayed beside figures that are reconciled and inheriting their credibility. Structural assertions hold it there, because a view that computes correctly today can compute wrongly tomorrow and no value test would notice | let the UI derive convenience totals | PROVEN (summing metrics in the view and serializing CSV in the browser were each provoked and observed failing) | Yes | Agent, under §18 |
| 2026-08-20 | WP-11 | **D-52 Export is a second rendering of one execution:** `GET /reports/run/:reportId/export` runs the SAME engine call as `GET /reports/run/:reportId` and serializes its `ReportResult`; nothing re-queries and the browser never assembles a file from the rendered table. §77 requires the database, API, screen, print and export to agree, and the reliable way is to make disagreement unrepresentable rather than merely tested. Authorization is identical to reading the report — an export is a read of the same numbers. The file carries its own provenance (report, period key, resolved span, branch scope, generated at/by), because a spreadsheet that has been emailed twice is otherwise impossible to place | let the client build the CSV from what it rendered; re-query in the export route | PROVEN (55 report×period combinations reconciled engine⇄API⇄CSV; perturbing the export by 1 broke 56 tests; verified live against a running server) | Yes | Agent, under §77 |
| 2026-08-20 | — | **D-53 One CSV serializer:** escaping is defined once in `server/src/utils/csv.ts` (RFC 4180: quote on comma/quote/CR/LF, double embedded quotes, CRLF rows). The students export was migrated onto it rather than the reporting export copying it — a second inline implementation is how one export learns to quote an embedded comma and the other does not | inline the escaping in each export | PROVEN (escaping tests; removing the escape broke 3; a metric note containing a comma and quotes round-trips) | Yes | Agent, under LAW 8 |
| 2026-08-20 | — | **D-54 The cleanliness audit had two blind spots, and under-reported:** (1) a regex literal containing a quote — `/[",\\n\\r]/` — desynchronized the scanner, which then treated the rest of the file as a string and skipped every later comment; (2) it scanned only tracked files, so a new file could introduce narrative and still pass. Both are fixed: regex literals are consumed as units (including after keywords such as `return`, where the character test alone reads as division), and the file list is `--cached --others --exclude-standard`. This means the C-4 count of 107 was an UNDERCOUNT — ten further narrative comments were hidden behind the first blind spot and are now rewritten | trust the earlier PASS | PROVEN (blind spot reproduced in isolation, then both failure modes provoked in real tracked and untracked files) | Yes | Agent, under LAW 9 and LAW 10 |
| 2026-08-20 | WP-02 | **D-55 Notifications obey the canonical authorities:** reads resolve through `resolveBranchScope` and cross-branch marking is authorized by `canAccessBranchResource` — the same helpers the other 74 route reads use. `users.branch_id` is an identity attribute that authorizes nothing (C-8), and scoping on it both hid other branches from an authorized owner and left `PATCH /:id/read` with no branch check at all. The default for an ordinary operator is unchanged (own branch); what changed is that an authorized request is now honoured and an unauthorized one is refused | add a notification-specific scope helper; leave the mutation unguarded because `user_id` is always NULL | PROVEN (9 tests failed against the defective behaviour, including `expected 200 to be 403` with the row left unmarked; five attacks provoked, each observed failing semantically) | Yes | Agent, under LAW 1, §22 and C-8 |
| 2026-08-20 | WP-02 | **D-56 Notification mutators are gated like the read:** `GET /` required at least one permission while `PATCH /:id/read` and `POST /read-all` required none, so a principal who could not read a notification could still mark it read. All three now carry `denyPermissionless`. Derived from the policy already stated on the same resource, not invented | leave the mutators open; introduce a new Notification permission code (would be inventing policy) | PROVEN (a role holding zero permissions was refused on all three; removing the gate reintroduced the failures) | Yes | Agent, under §22 |
| 2026-08-20 | WP-02 | **D-57 `read-all` reports rather than pretends — SUPERSEDED BY D-58:** at this checkpoint nothing wrote `notifications.user_id`, so `WHERE user_id = ?` matched nothing and the endpoint changed 0 rows while answering `{ ok: true }`. Before the owner resolved A-9.1, the response was made truthful without choosing policy. D-58 now supplies that policy and replaces the no-op implementation | redefine the semantics before owner decision; delete the used endpoint | PROVEN (`expected { ok: true } to have property "marked"` before this checkpoint) | Superseded | Agent, under LAW 6 and LAW 10 |
| 2026-08-20 | WP-02 | **D-58 Notification read state is per user:** owner selected per-user rather than shared-branch state. `notification_read_receipts(notification_id,user_id)` is the sole storage authority; absence means unread and the composite primary key makes marking idempotent. `notifications.read` is removed so reading can never mutate or suppress the event for another viewer. The unused `notifications.user_id` target column is also removed: no writer, dynamic path, script, test fixture or consumer populated it, and retaining a dead targeting model beside receipts would misstate the domain | shared read flag on the notification row; copy one notification per viewer | PROVEN (28 notification tests; schema preflight 111 tables / 228 indexes / 64 triggers; both typechecks) | No (canonical schema) | Owner (A-9.1 checkpoint) |
| 2026-08-20 | WP-02 | **D-59 An organization-scoped Owner's bell aggregates all authorized branches by default:** notifications call `resolveBranchScope(req,{defaultToAllAuthorized:true})`; there is no notification-specific authorization resolver. Branch-scoped users still default to their one authorized branch, and an explicit branch remains available to an organization-scoped caller. The UI sends no branch override and labels each row with its branch so an aggregated financial alert retains operational context | follow the active branch selector; aggregate in the browser | PROVEN (cross-branch API tests plus frontend typecheck) | Yes | Owner (A-9.2) |
| 2026-08-20 | WP-02 | **D-60 A branch identity is never a fallback authorization grant:** `resolveBranchScope` now checks `canAccessBranch` even when the requested/default branch equals `users.branch_id`. If no assignment authorizes that identity branch it fails 403 instead of returning the identity as scope. This closes the remaining path by which the canonical resolver itself contradicted C-8 while preserving the established fallback for an operator whose home branch is genuinely authorized | trust every user's home branch to match an assignment; repair only the notification route | PROVEN (`server/src/tests/work-packages/wp01/organization-configuration.authorization.test.ts`, including deliberately misaligned identity and assignment) | Yes | Agent, under C-8, LAW 1 and §22 |
| 2026-08-21 | WP-07 | **D-61 Owner-approved treasury policy and executable ceiling:** margin below 10% permits 0% distribution; 10–19.99% permits 5%; 20–29.99% permits 10%; 30% or more permits 15%. The reserve target is six months of fixed costs and must remain covered by `main_balance + saving_balance` after withdrawal. Main cash below three months of fixed costs warns; teacher performance below 80% warns. `computeProfitDistribution` publishes and enforces one current-accounting-month ceiling: the minimum of remaining period allowance, available main cash and liquidity above the reserve. The enforcing path recomputes it inside the same transaction as the conditional main-account debit and verifies the reserve again after debit. The disconnected editable `rule_default_reserve_guard` is removed rather than retained as a second policy surface | savings-only reserve; 20%/15%/10% distribution tiers; two-month cash warning; an editable rule-engine reserve guard; separate publish/enforce arithmetic | DECISION (owner), implementation verified by focused boundary, parity, concurrent-request and no-mutation tests | Yes | Owner (A-10 checkpoint) |
| 2026-08-21 | Operations | **D-62 Automated SQLite backups are required:** on process startup, create a backup when no current verified pair is less than 24 hours old; while running, schedule from that pair so attempts remain 24 hours apart. Retain 7 daily, 4 weekly and 12 monthly snapshots. A run succeeds only when SQLite's online backup API produces local and required external/network copies that both pass integrity check, foreign-key check, non-empty-file validation and identical SHA-256/size verification. On Windows, the external destination must be another drive or UNC path. Startup fails without it; a later failure makes health/readiness return 503, never local-only success | no automated backup; local snapshots only; raw copies of a live database | PROVEN (`database-backup.test.ts`: startup freshness, schedule, GFS retention, corruption, restore, copy failure and destination attacks) | Yes | Owner (A-11 checkpoint) |
| 2026-08-21 | WP-01 | **D-63 Organization-global administration follows assignment scope, not the Owner role label:** campus, partner and system-setting administration requires `owner@organization`. A branch- or campus-scoped Owner remains scoped; branch and academic operations use the canonical assignment reach | treat every Owner role assignment as global; authorize by `users.branch_id` | PROVEN (`organization-configuration.authorization.test.ts`) | Yes | Agent, under D-14, C-8 and §22 |
| 2026-08-21 | WP-01 | **D-64 Generic operational rule management does not own domain policies that already have an authority:** fee, promotion, attendance and academic categories are rejected by the generic management/evaluation adapter; their owning domain services remain responsible for any internal policy-engine use. Discount and payroll retain their established generic-rule consumers. The disconnected `rule_default_auto_savings` row is removed at startup, while `system_settings.daily_saving_percent` and `server/src/utils/income.ts` remain the unchanged live savings authority | retain overlapping editable rules; classify payroll as domain-owned despite its live generic consumer; restore the disconnected seed | PROVEN (`organization-configuration.integrity.test.ts`, `organization-configuration.architecture.test.ts`) | Yes | Agent, under LAW 1 and WP-01 checkpoint |
| 2026-08-21 | WP-01 | **D-65 Academic catalog scope follows the ownership graph:** branch ownership resolves through program → program version → level/subject/module; promotion, placement, fee snapshot, branch profile and class-generation inputs must agree with that graph. A caller authorized for two branches may operate in either, but cannot combine identifiers across them | trust caller-supplied branch ids independently; filter lists only | PROVEN (`organization-configuration.api.test.ts`, `organization-configuration.authorization.test.ts`, `organization-configuration.integrity.test.ts`) | Yes | Agent, under LAW 1, LAW 6 and §22 |
| 2026-08-21 | WP-01 | **D-66 WP-01 replaces its mapped legacy tests with package authorities:** the 10 inventoried files / 108 declared cases were removed after their organization/configuration knowledge was re-expressed in five `server/src/tests/work-packages/wp01/` suites. Placement-enrollment enforcement remains assigned to WP-04 rather than duplicated into configuration; detailed money-writer semantics remain assigned to WP-07 | retain the old files beside replacements; copy downstream domain suites into WP-01 | PROVEN (legacy inventory mapping; 116 package tests; deleted-file registry scan) | No (Git-recoverable) | Agent, under D-18 and §104 |
| 2026-08-21 | WP-02 | **D-67 Permission decisions are assignment-correlated and deny-dominant:** a role may contribute a permission to a branch only when that same live assignment reaches the branch. One position cannot lend a permission to another position's scope. An explicit deny deterministically removes the denied permission regardless of row order | check permission codes and branch reach independently; retain database-row-order conflict resolution | PROVEN (`identity-access.attack.test.ts`, `security-grant-escalation.test.ts`) | Yes | Agent, under D-14, LAW 1 and §22 |
| 2026-08-21 | WP-02 | **D-68 Account administration has a correlated privilege ceiling:** listing, editing, role mutation, override mutation and password reset require the caller's operation permission in the target branch, and takeover-capable mutations require every target permission in its own assignment boundary. Permission reach in branch B cannot authorize takeover or deny removal in branch A | compare permission-code sets without assignment scope; authorize by target home branch only | PROVEN (`identity-access.attack.test.ts`) | Yes | Agent, under D-14, LAW 1 and §22 |
| 2026-08-21 | WP-02 | **D-69 Linked staff identity lives on the account side only:** `users.linked_teacher_id` and `users.linked_employee_id` are unique foreign keys. The duplicate `teachers.user_id` and `employees.user_id` columns and their read/transfer fallbacks are removed, so a staff relationship cannot disagree in two directions | retain both pointers and synchronize them; make the entity-side pointer canonical | PROVEN (`identity-access.architecture.test.ts`, fresh-schema preflight) | No (canonical schema) | Agent, under LAW 1 and D-18 |
| 2026-08-21 | WP-02 | **D-70 Global Owner UI authority is a server-resolved fact:** authentication responses serialize `isGlobalOwner` from the live organization-scoped assignment. Frontend controls consume that fact or effective permission codes; no component infers superuser authority from the string `owner`, and mutable settings cannot impersonate the active position | keep Owner-label bypasses in components; derive global status independently in each client | PROVEN (`identity-access.architecture.test.ts`, frontend typecheck) | Yes | Agent, under D-34 and D-63 |
| 2026-08-21 | WP-02 | **D-71 WP-02 replaces its mapped legacy baseline with package authorities:** fourteen requirement suites are rebased under `work-packages/wp02`; WP-02 sections are removed from the two mixed legacy files while their downstream finance/audit and WP-03 search cases remain. Attack and architecture suites make the corrected authority explicit | retain all historical mixed narratives; delete downstream tests outside WP-02; weaken student-name and scope-correlation requirements to satisfy stale expectations | PROVEN (`docs/legacy-test-inventory.md`; 16 package files / 187 tests) | No (Git-recoverable) | Agent, under D-18 and §104 |
| 2026-08-21 | WP-02 | **D-72 Sessions carry identity, never authority:** the HttpOnly `SameSite=Strict` cookie carries only identity plus `session_version`; every request rebuilds live RBAC. Login conditionally records success after bcrypt so reset/deactivation races fail closed, password changes use optimistic session-version concurrency, and logout increments only the version represented by the submitted token so a stale token cannot revoke a newer session | carry role/permissions in JWT; fire-and-forget logout; unconditionally bump session version from stale tokens | PROVEN (`password-quarantine.test.ts`, `student-portal-secret-auth.test.ts`, `identity-access.attack.test.ts`, `identity-access.architecture.test.ts`) | Yes | Agent, under LAW 1 and §22 |
| 2026-08-21 | WP-02 | **D-73 Permission action scope is enforced separately from assignment reach:** `own` and `class` grants authorize only objects or filtered collections whose teacher/student/class relationship is proven. They cannot authorize a branch collection merely because their supplying assignment reaches that branch. Department-scoped HOD grants continue to use their live branch posting because the current resource model has no department identifier; `Dashboard.View:own` retains its established assigned-branch summary contract. Exam ownership has no canonical relation, so `Exam.View:own` fails closed | treat every effective permission code as branch-wide; deny all narrow-scope teacher workflows; infer exam ownership from a role label | PROVEN (`identity-access.attack.test.ts`, `rbac-search-entity-permission.test.ts`, full suite) | Yes | Agent, under §22, D-67 and established permission catalog scopes |
| 2026-08-21 | WP-03 | **D-74 Student profile lifecycle has one command path per effect:** ordinary active/inactive/graduated transitions use the guarded student lifecycle; suspension and resume use the enrollment service because they must update every live enrollment and the student profile together; a suspended profile cannot change to another status or create an alternate resume enrollment until that workflow resumes it. A suspension batch records the exact active semester ids deferred by that operation, so resume cannot activate unrelated deferred history; an unmapped historical suspension fails closed rather than restoring by student or class. Graduation remains terminal and completes active enrollment/semester rows | allow manual journey lifecycle events; update only one enrollment; resume every deferred semester in a matching class | PROVEN (`students-admissions.attack.test.ts`, `student-subsystem-remediation.test.ts`, dependent enrollment suite) | No (canonical schema mapping) | Agent, under LAW 1, LAW 6 and WP-03 checkpoint |
| 2026-08-21 | WP-03 | **D-75 Visitor conversion is an admission transaction, not a client-priced transfer:** conversion preserves the lead's branch, derives gross tuition from the selected active class, rejects an unauthorized discount transformation or unsupported payment method, and commits lead, student, semester, enrollment, invoice and payment effects in one transaction. Program-change placement invalidation is likewise transactionally coupled to the profile change | accept caller branch/fee as authority; silently cap discounts; commit the student before late eligibility checks | PROVEN (`visitors.test.ts`, `visitor-conversion-money-validation.test.ts`, `students-admissions.attack.test.ts`) | Yes | Agent, under LAW 1, LAW 5 and LAW 6 |
| 2026-08-21 | WP-03 | **D-76 Student journey is chronology, not shadow state or ledger:** the stream remains append-only, but current student, enrollment, placement snapshot and card facts are overlaid from owning tables and tuition comes from `studentBalance`. The manual endpoint accepts bounded notes only. Financial event rows and nested money-shaped payload fields require `Payment.View` | project money and lifecycle exclusively from events; retain manual status/graduation/placement writers; redact only top-level fields | PROVEN (`timeline-separation.test.ts`, `students-admissions.attack.test.ts`) | Yes | Agent, under LAW 1, §22 and WP-03 checkpoint |
| 2026-08-21 | WP-03 | **D-77 Student identity uniqueness is enforced at application and database boundaries:** student and converting-lead phone syntax/comparison use the same canonical phone authority, normalized email is unique, and unrelated visitor/student Tazkira identities cannot coexist. A converted student may retain the Tazkira of its linked originating visitor | compare phone formatting literally; rely only on route prechecks; reject the linked visitor during conversion | PROVEN (`student-subsystem-remediation.test.ts`, `visitor-subsystem-audit.test.ts`, `students-admissions.attack.test.ts`) | No (canonical schema indexes/triggers) | Agent, under LAW 1 and LAW 6 |
| 2026-08-21 | WP-03 | **D-78 WP-03 replaces its mapped legacy baseline with package authority:** all fourteen inventoried suites are rebased under `work-packages/wp03`; the adversarial suite covers corrected lifecycle, scope, input, redaction, conversion and exact-semester restoration behavior. Obsolete role-label permission substitutes, manual journey mutation, payment-method fallback and single-row suspension expectations are retired rather than retained beside the corrected model | leave suites in the shared legacy directory; preserve contradictory expectations; copy later Placement/Finance suites into WP-03 | PROVEN (`docs/legacy-test-inventory.md`; 15 mapped files / 355 tests plus 1 supplemental UI file / 1 test) | No (Git-recoverable) | Agent, under D-18 and §104 |

## Tracked risks (READY WITH TRACKED RISK items)

| # | Risk | Severity | WP | Owner | Target |
|---|---|---|---|---|---|
| TR-1 | Bidirectional text embedding unverified: Persian labels and Persian-Indic digits inside LTR containers have no test asserting punctuation/digit ordering | Low | WP-11 | Owner | next UI pass |
| TR-2 | ~~Money displayed with 0 decimals while stored with 2~~ | — | WP-07 | — | **CLOSED** by D-22: storage and display are now the same unit, so a total cannot disagree with the sum of its displayed parts |
| TR-3 | No `POST /finance/budget-lines` audit of *deactivation* flow in the UI — the API supports `isActive:false` but no screen exposes it | Low | WP-07 | Owner | next Finance UI pass |
| TR-4 | INDEPENDENT REVIEW is performed by the same agent; mitigated by artifact-only review and executable findings, not eliminated | **High** (raised from Medium) | — | Owner | Structural; needs a second reviewer. **2026-08-21: the agent formally declined to self-certify** — see `docs/work-packages/WP-07-TR4-independent-review-dossier.md`. Two self-authored artifacts in one session were green and wrong: the F-18b analysis (D-152) and the first C-2 money-boundary guard. A reviewer dossier plus objective mutation-harness findings are prepared; **TR-4 remains OPEN**. **2026-08-22: Owner ruling — no self-review, no §R exception.** The reviewer package is handed off to a genuinely independent agent/session (branch `arena/01a0299b-toefl-house`, baseline `102074c`, gates re-measured green at handoff); every disposition applied to date was applied by the implementing agent and is now a claim awaiting that reviewer's execution-backed verdict — see `docs/work-packages/WP-07-TR4-handoff-to-independent-reviewer.md`. **TR-4 remains OPEN pending the independent verdict** |
| TR-5 | ~~Automated backups are required but absent~~ | — | Operations | — | **CLOSED** by D-62: startup and 24-hour scheduling publish only hash-identical, integrity-verified local/external pairs and enforce GFS retention |
| TR-7 | ~~`impact.routes.ts` declared its own Gregorian `periodBounds()`, a second period authority~~ | — | WP-11 | — | **CLOSED** by D-45: the duplicate resolver is deleted, impact resolves through `periodBoundariesForKey`, and a source scan fails the build if any route re-declares one |
| TR-6 | ~~A consistent SQLite snapshot had no restore assertion after migration-test removal~~ | — | Operations | — | **CLOSED** by `database-backup.test.ts`: the online snapshot remains point-in-time consistent, both destination hashes match, and an offline restored copy is byte-identical to the verified snapshot and passes SQLite/FK integrity |
| 2026-08-21 | WP-04 | **D-79 Placement profile stores policy facts once:** `requirement_mode`, component JSON, scoring model, pass score, retake/billing controls, expiry and canonical level-id decision rules are stored; enabled/required/method/sections/overall maximum are projections. Candidate branch → program-owning branch → global is the sole lookup hierarchy, and malformed persisted facts fail closed before a sitting starts | restore duplicate compatibility columns; let UI projections become write authorities; choose the first profile found | PROVEN (`profile-policy.integration.test.ts`, `attack.test.ts`, canonical schema) | No (canonical schema) | Agent, under LAW 1, LAW 6 and established placement contracts |
| 2026-08-21 | WP-04 | **D-80 A placement sitting owns an immutable snapshot and wall-clock deadline:** policy, content, answer keys, rubric and billing/prior-attempt facts are captured at start. Operational projections redact answer keys. Component pause/resume may shift component deadlines; it never extends overall `expires_at`, and one timing engine owns expiry/reset | score against mutable authoring rows; trust client timers; let pause extend overall expiry; reset the visitor in multiple layers | PROVEN (`attempt-lifecycle.integration.test.ts`, `test-bank-security.integration.test.ts`) | Yes | Agent, under LAW 1, LAW 6 and §83 |
| 2026-08-21 | WP-04 | **D-81 Server scoring and one decision engine own every placement result:** automatic points come from stored responses, manual/hybrid portions are bounded, weighted values use configured component maxima, conditional rules precede score bands, and the authoritative outcome is persisted. Corrections recompute the full result/projection transactionally while preserving an explicit authorized override and provenance | accept aggregate client scores; duplicate pass/fail in conversion; correct one result row without recomputation | PROVEN (`scoring-decision-correction.integration.test.ts`; score constraints exposed and reproduced a weighted-score arithmetic defect before repair) | Yes | Agent, under LAW 1, LAW 5 and LAW 6 |
| 2026-08-21 | WP-04 | **D-82 Retake permission, attempt cap and billing are independent snapshotted facts:** first sitting and retake billability are evaluated separately; an optional retake fee falls back to the canonical branch placement fee. Completion writes one idempotent payment plus linked income only when billable, including failed sittings | infer billability from retake permission; read current configuration at completion; make failed sittings free; use a compatibility charge path | PROVEN (`retake-billing.integration.test.ts`) | Yes | Agent, preserving established configuration without adding policy |
| 2026-08-21 | WP-04 | **D-83 Placement assets use object-correlated authority and explicit CAS/lifecycle commands:** a role, home branch or permission somewhere never authorizes a foreign object; global templates require organization-global Owner for mutation; test/rubric branch and kind remain correlated; content updates use versions and status changes use activate/archive endpoints; media confinement checks both lexical and real filesystem paths | authorize by role/home branch; accept caller status in generic PUT; trust extension/MIME headers or normalized paths alone | PROVEN (`test-bank-security.integration.test.ts`, `attack.test.ts`) | Yes | Agent, under §22, LAW 6 and existing test-bank policy |
| 2026-08-21 | WP-04 | **D-84 Placement eligibility is enforced at every admission boundary:** conversion and both shared class-enrollment consumers resolve the governing class/program policy and require an authoritative pass, established waiver, or not-required mode. Unlinked students and policies outside the applicable hierarchy fail closed | enforce only visitor conversion; trust `visitors.placement_status` without the latest attempt; let callers supply a fallback program | PROVEN (`enrollment-gates.integration.test.ts`, dependent enrollment suites) | Yes | Agent, under LAW 1 and LAW 6 |
| 2026-08-21 | WP-04 | **D-85 WP-04 replaces its historical runtime contract:** all 13 historical placement suites remain explicit skipped knowledge records; seven focused behavior suites replace their mapped knowledge, and one architecture suite pins authority/schema/UI boundaries. The untouched Academic Delivery assessment engine remains WP-06 and is not claimed by WP-04 | weaken CAS/scope/schema to preserve obsolete fixtures; count WP-06 as placement; leave contradictory historical runtime active | PROVEN (`docs/legacy-test-inventory.md`; 7 files / 60 mapped cases plus 8 architecture cases; full server suite 2513 passed / 160 explicitly skipped) | No (Git-recoverable) | Agent, under §104 and WP-04 checkpoint |
| 2026-08-21 | WP-05 | **D-86 Academic ownership identifiers form one graph:** a program owns its versions and levels, the program owns the branch, a subject's optional level belongs to its version, and an offering/class may reference only a mutually consistent program, version, level, branch and term. Setup writers reject invalid structural scalars rather than coerce or silently substitute them; direct storage receives correlation backstops | treat each foreign key as independently valid; rely only on forms/routes; preserve negative/coerced planning values | PROVEN (`server/src/tests/work-packages/wp05/attack.test.ts`; canonical schema preflight; 17-file/348-case WP-05 authority) | No (canonical schema) | Agent, under LAW 1, LAW 6 and WP-05 checkpoint |
| 2026-08-21 | WP-05 | **D-87 Offering capacity is derived, not stored:** `capacityTotal` is the sum of capacities of classes currently linked to the offering. The `course_offerings.capacity_total` duplicate and generation-only refresh path are removed; manual links and later class edits cannot create stale capacity | keep the stored field and update it from every possible class writer; trust only generation-created classes | PROVEN (`attack.test.ts`; derived offering reads; removed storage column) | No (canonical schema removal) | Agent, under LAW 1 and §53 |
| 2026-08-21 | WP-05 | **D-88 Transfer and freeze are exact atomic commands:** a transfer names one active source enrollment, moves that row, closes only its matching active semester projection and opens the destination projection. Transfer/freeze lifecycle effects, events and request/history rows share one transaction; a late history failure rolls back the lifecycle effect | select the latest active enrollment by student; complete every active semester; persist request history after service commit | PROVEN (`attack.test.ts`; `transfer-freeze-waitlist-engines.test.ts`; exact-source CAS and rollback attacks) | Yes | Agent, under LAW 1, LAW 5, §49 and WP-05 checkpoint |
| 2026-08-21 | WP-05 | **D-89 Waitlist offer means an available FIFO seat:** an entry can be offered only when class occupancy is below its positive capacity and no earlier waiting entry exists. Conversion rechecks the seat transactionally and delegates enrollment, semester and future-roster projections to `EnrollmentService` rather than writing a second projection path | offer while full; allow staff to skip the queue; retain manual semester/roster inserts | PROVEN (`attack.test.ts`; replaced FIFO/full-class authority in `transfer-freeze-waitlist-engines.test.ts`) | Yes | Agent, under LAW 1, LAW 6 and WP-05 checkpoint |
| 2026-08-21 | WP-05 | **D-90 Teacher performance has evaluation provenance and deactivation has one guard:** generic teacher edit cannot write `performanceScore`; the evaluation command writes the evaluation and score projection atomically. Every request to set a teacher inactive, including generic PUT, refuses while active direct classes or ongoing skill assignments remain | keep two score writers; guard DELETE/transfer but not PUT; fabricate an evaluation-free score | PROVEN (`attack.test.ts`; teacher evaluation/update suites; scoped mutation runs 3/3 killed) | Yes | Agent, under LAW 1, LAW 5 and WP-05 checkpoint |
| 2026-08-21 | WP-05 | **D-91 Teaching assignments are workload facts with database correlation:** class, teacher, assignment and optional session share a branch; the session belongs to the class; rate is a non-negative whole AFN value; end is not before start; class-scoped NULL-session identity is unique; and at most three distinct ongoing primary/assistant skills exist per class. Compensation calculation remains WP-08 | application-only checks; nullable composite uniqueness; let payroll semantics define whether teaching work exists | PROVEN (`attack.test.ts`; `teacher-assignment-engine.test.ts`; route and storage correlation) | No (canonical schema constraints) | Agent, under LAW 1, LAW 6 and WP-05 checkpoint |
| 2026-08-21 | WP-05 | **D-92 WP-05 uses the Protocol route boundary, not stale inventory headings:** enrollment/freeze/transfer/waitlist behavior belongs to WP-05; generic assessments/sessions/attendance remain WP-06; teacher payroll remains WP-08. The 15-file/229-declared-case historical core must be replaced or explicitly retired, while mixed teacher input/skill files are split by behavior rather than claimed wholesale | preserve the old 20-file WP-05 and 13-file WP-06 headings; move all teacher-route tests into WP-05; certify contradictory legacy behavior | PROVEN (`docs/legacy-test-inventory.md`; §W; 327 rebased historical plus 21 focused attack/graph cases) | No (Git-recoverable) | Agent, under §58, §W and §104 |
| 2026-08-21 | WP-06 | **D-93 WP-06 uses the Protocol route boundary, not stale inventory headings:** sessions and exams routes plus the WP-05-excluded assessment/gradebook/day-level-attendance behavior hosted in classes.routes are WP-06; installment-plan validation lives on students.routes (WP-03) and its suite is re-homed there, not claimed. Ten inventoried suites (94 declared cases) rebase to `server/src/tests/work-packages/wp06/` and four authority suites are added | preserve the 11-file heading; move student-route behavior into WP-06; leave the assessment engine split | PROVEN (inventory C-16; 14 files / 126 tests in wp06; full suite 163 files / 2564 tests with only the 160 explicit WP-04 retirements) | No (Git-recoverable) | Agent, under §W, §58, §104 and D-92 precedent |
| 2026-08-21 | WP-06 | **D-94 Session attendance has one store and one read union:** session marks live in `rosters` only — the `attendance` mirror (with its three proven drift paths: the single-mark PATCH that never mirrored, the day-level delete-by-date+target that removed session rows, and session deletion that orphaned them) is removed, along with the `attendance.session_id` column. `attendance` keeps only session-less marks (day-level student + teacher). Every read surface — report metrics `attendance.present`/`attendance.recorded`, the impact stats, `/api/attendance` list and summary — consumes `server/src/core/academic/attendance-query.ts`, the one union expression. Teacher rows are excluded from student surfaces (they were polluting the attendance report), and the summary rate now credits the canonical attended-equivalent statuses | keep the mirror and add reconciliation; keep teacher rows in student metrics; keep present+leave as a second credit definition | PROVEN (`server/src/tests/work-packages/wp06/attendance-authority.test.ts` 9 cases; full suite; schema preflight) | No (canonical schema) | Agent, under LAW 1, §18, §77 and WP-06 checkpoint |
| 2026-08-21 | WP-06 | **D-95 Certificate revocation is a state transition, never a deletion:** `certificates` gains `status` ('issued'/'revoked') with `revoked_at`/`revoked_by`; a downward correction revokes instead of deleting, and a later upward correction issues a new numbered certificate exactly as before. The once-per-student diploma-fee rule and the no-money-reversal policy are preserved verbatim. Count surfaces (`academic.certificates_issued` metric, reports overview) count only issued output | delete the row on revocation; refund the diploma fee on revocation | PROVEN (3 rebased assertions + `server/src/tests/work-packages/wp06/exam-scope-and-storage.test.ts` metric case; schema preflight) | No (canonical schema) | Agent, under D-17, LAW 4, §8 and WP-06 checkpoint |
| 2026-08-21 | WP-06 | **D-96 `status` is the scored marker, and the exam vocabulary is one:** `exam_results.status` is NOT NULL CHECKed ('pending'/'pass'/'fail') and `score` is CHECKed 0–120; the score-entry gate reads `status !== 'pending'`, so a legitimately recorded 0 cannot be overwritten and the UI renders scored state from status. The `'mock'` exam type — admitted by the schema CHECK and default and by the frontend union but rejected by the API and never written by anything — is removed everywhere | keep score>0 as the scored marker; add 'mock' to the API | PROVEN (`server/src/tests/work-packages/wp06/exam-scope-and-storage.test.ts`; full suite; both typechecks) | No (canonical schema) | Agent, under LAW 1, LAW 3, LAW 7 and WP-06 checkpoint |
| 2026-08-21 | WP-06 | **D-97 Session history is immutable and session input is real:** DELETE refuses any session with recorded marks; PUT refuses date/time changes on completed sessions; the single-roster PATCH becomes the same transactional command as the bulk path (mark + auto-drop + `attendance.marked` event in one transaction, completed-session corrections manager-only); `isoDate.ts` gains the canonical time-of-day authority and every session writer validates real ISO dates and HH:MM times, with the timetable generator rejecting out-of-range weeks/days | allow cascade deletion of marked sessions; let PATCH stay a second, weaker command; store free-form times | PROVEN (`server/src/tests/work-packages/wp06/attendance-authority.test.ts`, `server/src/tests/work-packages/wp06/session-input-guards.test.ts`; full suite) | No (canonical schema for attendance column removal) | Agent, under LAW 3, LAW 6, §8 and WP-06 checkpoint |
| 2026-08-21 | WP-06 | **D-98 Exam creation honors assignment scope:** POST /exams resolves its branch through `resolveBranchScope` — the identity branch is never a fallback creation grant, an explicit authorized scope wins, and an explicit all-branches request is refused because an exam needs one branch | keep `req.user.branchId` as the creation branch | PROVEN (`server/src/tests/work-packages/wp06/exam-scope-and-storage.test.ts` misaligned-identity case; full suite) | Yes | Agent, under C-8, D-60, §22 and WP-06 checkpoint |
| 2026-08-21 | WP-06 | **D-99 Grade and assessment bounds are enforced at both boundaries:** scores are non-negative and bounded by the assessment max score, weights non-negative, max scores positive, grade statuses restricted to the canonical four — enforced in the route and backstopped by CHECKs on `student_grades` and `class_assessments` | keep route-only checks; widen the grade status vocabulary | PROVEN (`server/src/tests/work-packages/wp06/gradebook-bounds.test.ts`; schema preflight; full suite) | No (canonical schema) | Agent, under LAW 3, LAW 6 and WP-06 checkpoint |
| 2026-08-21 | WP-06 | **D-100 WP-06 replaces its mapped legacy baseline with package authority:** the ten inventoried suites rebase to `server/src/tests/work-packages/wp06/` (the two certificate suites' assertions re-expressed for revocation-as-state), `installment-plan-integrity.test.ts` re-homes to `wp03`, and four new suites pin the attendance union, session input guards, exam scope/storage and gradebook bounds. The full clean-data suite passes 163 files / 2564 tests with only the 160 explicit WP-04 retirements | leave suites in the shared legacy directory; preserve the deletion-based certificate expectations; claim the installment suite | PROVEN (`docs/legacy-test-inventory.md`; 14 files / 126 tests in wp06) | No (Git-recoverable) | Agent, under D-18, D-93 and §104 |
| 2026-08-21 | WP-07 | **D-101 A budget movement is one SIGNED `budget_charge` row, written by one authority:** `server/src/core/finance/budget-movements.ts` is the only writer of money into or out of a budget line that is not spend. Funding is positive; a month-end return and the outgoing leg of a line-to-line transfer are negative; the incoming leg is positive; `reference_id` is always the line that moved and `branch_id` is always the branch that owns it. A transfer is therefore two movements that sum to zero, and `saving_transfer` is left to mean the branch savings account alone. The writer also carries `allocated_amount` with the movement, so `allocated − current` is spend rather than "money ever placed here" | keep three per-route writers and teach the reconciler about each; add a second ledger type for returns; leave `allocated_amount` cumulative and correct the utilization metric instead | PROVEN — three defects reproduced before the change (return: `budgetVariance = −20,000`; transfer: `cashVariance = +8,000` / `savingVariance = −8,000` with 8,000 AFN of phantom savings; cross-branch settlement booked to the operator's branch) and pinned by `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` (32 cases) | Yes (convention change; no schema change) | Agent, under LAW 1, LAW 4, LAW 7, §21 and §78 |
| 2026-08-21 | WP-07 | **D-102 Budget movements are disclosed by the operator action that produced them:** `/finance/pnl` and `/reports/overview` publish `budgetCharged`, `budgetReturned` and `budgetTransferred` instead of one netted "budget charged" figure, and the ledger row's arrow is derived from the amount's sign rather than from its type | publish the signed net under the existing name; keep gross-by-sign without naming the action | PROVEN (`budget-movement-authority.test.ts` P&L case; both typechecks; frontend consumers updated) | Yes | Agent, under §18, §30 and §77 |
| 2026-08-21 | WP-07 | **D-103 The savings rate is validated where it is written and refused where it is read:** `PUT /api/finance/saving-engine/settings` parses a number in 0–100 (a comparison-only guard stored `'abc'` verbatim, after which every read silently fell back to the default rate), and `recordIncome` fails with 409 on a stored value outside that range instead of clamping it | keep the clamp; coerce on read; accept any value and normalize during the sweep | PROVEN (`budget-movement-authority.test.ts` savings-rate cases) | Yes | Agent, under LAW 6, §37 and §51 |
| 2026-08-21 | WP-07 | **D-104 Money comparisons are exact whole AFN:** the two-decimal residue in the finance authorities (three `round2` helpers and the `0.01` reconciliation tolerances) is removed, so a one-afghani break is reported rather than tolerated, and rounding keeps its single implementation in `server/src/utils/money.ts` | keep the tolerance as defence against future fractional storage; re-round defensively at each read | PROVEN (`budget-movement-authority.test.ts` one-afghani case; full suite 2596 passed) | Yes | Agent, under LAW 1, LAW 7 and D-12/D-22 |
| 2026-08-21 | WP-07 | **D-105 The release gate's financial reconciliation runs a real money lifecycle:** the probe drives income, the savings sweep, a refund that reclaims from savings, capital funding, budget spend, a line-to-line transfer and a return, then requires every variance to be exactly zero and the surviving allocation to equal the spend. An empty-database probe proved only that 0 = 0 and could not have detected D-101's defects | keep the empty-database probe and rely on the unit suite; add a separate non-gating script | PROVEN (`npm run release:validate` step reports "full money lifecycle · amount/cash/saving/budget all 0") | Yes | Agent, under §67, §74 and §78 |
| 2026-08-21 | WP-07 | **D-106 Finance operational settings have one validation authority:** `server/src/core/configuration/finance-settings.ts` owns the key, field name and parse for `daily_saving_percent`, `expense_auto_approve_threshold` and `invoice_due_days`, and all three writers (`PUT /api/finance/saving-engine/settings`, `PUT /api/finance/expense-auto-approve-threshold`, `PUT /api/invoices/config/settings`) consume it. A value that fails validation is a 400 that names the field, never a silent skip behind a 200, and the response reports what was applied | keep per-endpoint checks and document the differences; collapse the three endpoints into one (changes three UI call sites and two contracts for no correctness gain) | PROVEN — `dailySavingPercent: 500` was accepted by the configuration form while the savings control refused it, and an invalid threshold was skipped behind `{ok:true}`; both pinned in `server/src/tests/work-packages/wp07/budget-movement-authority.test.ts` | Yes | Agent, under LAW 1, LAW 6, §37 and §51 |
| 2026-08-21 | WP-07 | **D-107 An envelope may not hide money:** a budget line holding a balance cannot be retired (409 naming the balance) and a retired line cannot be funded (409, treasury debit inside the same transaction). This is what makes it safe for the finance dashboard to report only operated lines, which is what the metric registry always claimed it did | let retirement park money out of sight and widen the dashboard to all lines; keep the dashboard unfiltered and correct the registry instead | PROVEN (`budget-movement-authority.test.ts` retirement cases; reconciliation stays healthy across both) | Yes | Agent, under LAW 6, §13 and §77 |
| 2026-08-21 | WP-07 | **D-108 An invoice payment refuses what it cannot record faithfully:** an unrecognised `paymentMethod` is a 400 rather than a silent `cash` (the rule `POST /api/students/:id/payments` already applied); the idempotency replay lookup — pre-check, in-transaction re-check and unique-violation branch — is scoped to the invoice and student, so a key already spent elsewhere yields 409 instead of a fabricated 200 replay carrying another payment's receipt; the replay check runs BEFORE the invoice-state gate so a retry of a payment that settled the invoice replays instead of reporting failure; and the ±0.001 comparison tolerances are removed | keep the cash fallback and document it; keep the global key lookup and rely on fingerprint uniqueness; keep the state gate first and accept the misleading 400 on retries | PROVEN — `cheque` recorded 2,000 AFN as cash; a shared `Idempotency-Key` answered 200 on a second, unpaid invoice while returning the first invoice's receipt number; a concurrent retry answered 400 "Only issued, partial, or overdue invoices can accept payment" although the money had been taken. All three are pinned by `server/src/tests/work-packages/wp07/invoice-payment-boundary.test.ts` | Yes | Agent, under LAW 1, LAW 4, LAW 6, §36 and §41 |
| 2026-08-21 | cross-cutting (WP-07/09/10, WP-08 precedent) | **D-109 An idempotency replay must be a replay of the same business event:** every money writer that accepts a caller-supplied `Idempotency-Key` checks that the record the key names is the same event the request describes — invoice payment by invoice and student, book sale by book and branch, donation by donor, campaign and amount — at the pre-check, at the in-transaction re-check and at the unique-violation backstop. A key that belongs to a different event is 409, never a 200 replay. Payroll already applied this rule and is the precedent | trust the derived fingerprint and ignore client keys; drop client keys entirely (breaks legitimate retry control); rely on the global unique index alone | PROVEN — before the change, a key spent on donor A's gift answered **200 with donor A's receipt number** for a request naming donor B (no donation recorded), and a key spent on book A answered 200 for a sale of book B (no stock moved, no income row). `server/src/tests/idempotency-replay-scope.test.ts` fails on the pre-change routes (5/9) and passes on the repaired ones (9/9) | Yes | Agent, under LAW 4, LAW 6, §36, §41 and §106 (deliberate cross-package re-scope, documented rather than ignored) |
| 2026-08-21 | WP-07 | **D-110 A document number is issued by one authority and is unique at rest:** `server/src/utils/documentNumbers.ts` owns "next number in this series" (`allocateDocumentSequence`); `receipt.ts` and `invoice.ts` become typed callers instead of three copies of read-add-pad. The database now refuses a duplicate: `uq_payments_receipt_number` (global, matching the single global receipt counter) and the existing per-branch invoice index. Series keys and rendered formats are unchanged, so no live sequence restarts | leave three generators and rely on the counters being atomic; make receipt numbers unique per branch (contradicts the global counter that issues them) | PROVEN — before the change the database accepted two payments carrying one receipt number, on insert and on update; `server/src/tests/work-packages/wp07/document-and-authorization-integrity.test.ts` (85 WP-07 cases in the package) | Yes | Agent, under LAW 1, LAW 3, §23 and §53 |
| 2026-08-21 | WP-07 | **D-111 A discount grant is parsed, dated and never silently substituted:** `assertPercent` (one percentage boundary in `server/src/utils/money.ts`, also consumed by the savings rate) replaces `Number()` coercion, the effective window goes through the canonical `isoDate` authority with an ordered-range check, and `resolveAuthorizedDiscount` no longer swallows SQL errors as "table absent" — a store it cannot read is a fault, not evidence that the student has no authorization | keep the coercion and rely on the category ceiling to catch nonsense; keep the pre-migration catch as defensive programming | PROVEN — `[10]`, `true`, `''` and `null` all produced authorization rows (0% records from empty fields), `effectiveFrom: 'banana'` was stored and silently disabled the grant, and a renamed table made the resolver charge ordinary policy in silence | Yes | Agent, under LAW 6, LAW 7, §37, §51 and §86 |
| 2026-08-21 | WP-07 | **D-112 A structural guarantee is asserted by its property, not by object name:** the WP-02 high-assurance case that listed five trigger names now asserts that every cross-branch-writable table has an insert guard and an update guard that raise on a branch mismatch. Forced by D-110/C-18; the WP-02 guarantee is unchanged and strictly better expressed, and the change is recorded rather than made silently | leave the obsolete names in place and keep the subsumed triggers alive purely to satisfy a test | PROVEN (case passes against the consolidated schema; WP-07 suite proves cross-branch writes are still refused) | Yes | Agent, under §14, §70 and §106 |
| 2026-08-21 | WP-07 | **D-113 A refund reverses one named payment (OWNER DECISION):** `POST /api/students/:id/refund` requires `paymentId`; the refund is capped at that payment's un-refunded remainder, stores the link in `payments.refunds_payment_id`, and the canonical balance authority counts a refund against tuition only when the payment it reverses is a tuition charge. `GET /api/students/:id/refundable-payments` publishes the remainder per payment so the browser never derives it | refund by stated category; tuition-only refunds; leave refunds unattributed and exclude them from tuition | PROVEN — before the change, refunding a 2,000 AFN exam fee left a fully-paid student showing 2,000 AFN of tuition debt that the enrolment debt-hold acts on; `server/src/tests/work-packages/wp07/refund-attribution-authority.test.ts` (16 cases) pins the repair, and the database refuses an unattributed refund even when the route is bypassed | Yes (additive column; A-1 holds) | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-114 A tuition refund re-opens only its own semester (OWNER DECISION):** the refund inherits the semester of the payment it reverses rather than accepting one from the caller, so the two can never disagree, and a refund of a non-tuition charge carries no semester and re-opens nothing | ask the operator to type the semester (can contradict the payment); apply refunds to the student's tuition overall | PROVEN (`refund-attribution-authority.test.ts` semester cases; the WP-03 semester-settlement suite re-expressed accordingly) | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-115 Fixtures that create refunds state what they reverse:** seven suites (WP-03 balances/idempotency, balance-single-source-of-truth, final-hardening, finance-money-writer-parity, system-closure-authorities and the visitors reset) now attribute their refunds and delete reversing rows before the rows they point at. Behaviour under test is unchanged; only the expression is. The stale claim "refunds carry no semester" is replaced by the D-114 rule and a new case proving a non-tuition refund never re-opens a term | weaken the database guard so old fixtures keep passing | PROVEN (full suite 2675 passed · 160 known skips) | Yes | Agent, under §14, §70 and §106 |
| 2026-08-21 | WP-07 | **D-116 One implementation of "how much has this semester been paid":** `getSemesterTuitionPaid` in the balance authority replaces the two inline copies inside the payment desk, which counted EVERY refund against whichever semester was being paid. Charges and refunds are both filtered by the semester itself, which is exactly what D-114 makes possible | keep the inline filters and patch each to check the refund's semester (three copies of one rule, drifting again on the next change) | PROVEN — WP07-F15: with Term A partially refunded, Term B's debt read 12,000 instead of 10,000, so after paying Term B in full the desk still accepted another afghani against it (`amountCharged: 1`). Pinned by `server/src/tests/work-packages/wp07/refund-attribution-authority.test.ts` | Yes | Agent, under LAW 1, LAW 4 and D-114 |
| 2026-08-21 | WP-07 | **D-117 Explicit payment allocation to a tuition obligation (OWNER DECISION — recorded, implementation deferred by the owner):** every tuition payment — direct, invoice or installment — must be explicitly allocated to the specific tuition obligation it settles. The installment plan is tied to its enrolment/semester obligation, so the operator does **not** select a semester when paying an installment. "Oldest unpaid semester" is explicitly rejected as an automatic rule | operator names the term per installment; each installment carries a semester in the JSON plan; automatic oldest-first allocation | DECISION (owner). The defect that prompted it, WP07-F16, is PROVEN: an installment stored with `semester = NULL` let 14,000 AFN be collected for a 10,000 AFN term | n/a — not yet implemented | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-118 Invoice → Obligation → Payment allocation (OWNER DECISION — recorded, implementation deferred by the owner):** an invoice declares its purpose and a tuition invoice identifies the specific tuition obligation it bills; books, exam and other purposes never reduce tuition; an invoice payment is never classified as tuition merely because it came through the invoice system, and the generic `payment.category = 'fee'` is not the mechanism | keep invoices tuition-only; make invoice payments never tuition | DECISION (owner). The defect that prompted it, WP07-F17, is PROVEN: a "Textbooks and stationery" invoice reduced tuition outstanding from 10,000 to 7,000 | n/a — not yet implemented | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-119 The allocation model is analysed before it is proposed:** per owner instruction, WP-07 produced a read-only verification of whether the current model can carry D-117/D-118 (`docs/work-packages/WP-07-payment-allocation-analysis.md`) instead of implementing. It records what already supports the direction, eight structural gaps with evidence, three candidate models assessed against the owner's rules, and six questions the owner must answer before any schema change | implement the smallest symptom fix immediately | FACT (analysis performed at `a17fcbd`; no source, schema or test changed; gate 22/22) | Yes | **Owner** instruction, executed by Agent under §61, §62 and §105 |
| 2026-08-21 | WP-07 | **D-120 One settlement authority: `student_obligations` + `obligation_allocations` (OWNER DECISION, implemented):** an obligation is what a student owes and an allocation is money applied to it by one instrument; `source_kind` is `payment` or `scholarship` with exactly-one-of enforced by CHECK. The obligation stores no amount — tuition's amount stays on the semester row, its existing authority (§13). A scholarship allocation settles the obligation and writes no cash and no ledger row, because the donor's money was recognised when the donation arrived; recognising it again as tuition would count one afghani twice and would break the derived branch cash position | scholarship as a payment method (would either double-recognise income or appear as an unmatched payment); scholarship as a discount on the obligation (erases the donor) | PROVEN — `server/src/tests/work-packages/wp09/scholarship-funding-authority.test.ts` (the active successor of the 24-case historical authority): a settled term reports `settledScholarship`, branch cash and the ledger are unchanged, reconciliation stays healthy, and the term cannot be collected again in cash | Yes (additive tables; A-1 holds) | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-121 A scholarship fund is backed only by donations explicitly allocated to it (OWNER DECISION, implemented):** `scholarship_fundings` records donation → fund; `received/committed/available` are derived, never mirrored; an award is bounded by `available`, so a fund nobody funded can award nothing — which is how "no institution-funded scholarships" is enforced without making any column mandatory. `scholarships.total_budget` becomes the declared target and bounds nothing | derive backing from a campaign's `raised_amount` (double-counts when a campaign funds several scholarships); owner-declared opening balance (a declaration is not money) | PROVEN — an unfunded fund with a 50,000 declared target awards nothing; a donation cannot be allocated beyond its own value; two concurrent awards cannot together exceed the fund | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-122 Two mechanics recorded rather than assumed:** (I-1) a reversed allocation returns to **its award**, and an explicit close-award returns the unapplied remainder to the **fund** — returning to the fund while the award stayed active would let active commitments exceed received backing; (II-2) `scholarships.total_budget` is retained as a declared target rather than removed, because removing it would change a UI contract the owner did not ask to change. Both are flagged for owner confirmation and neither was chosen silently | return a reversal straight to the fund (breaks the commitment invariant); delete `total_budget` | PROVEN (`scholarship-funding-authority.test.ts` reversal and close cases) | Yes | Agent, under §90 and LAW 6 |
| 2026-08-21 | WP-07 | **D-123 Two funding source-text cases re-expressed:** `funding-money-validation.test.ts` pinned the retired declared-budget ceiling (`Number.isFinite(totalBudgetValue)`, `awardAmount <= remaining`, the `allocated_amount` running total). The invariant they protect — a non-real budget can never approve an award, and the persisted amount is the validated one — is now strictly stronger, because `total_budget` no longer participates. The cases assert the new authority and point at the behavioural proof | delete them; weaken D-121 to keep them passing | PROVEN (12/12 in that file; behaviour proven in the WP-07 package suite) | Yes | Agent, under §14, §70 and the D-112/D-115 precedent |
| 2026-08-21 | WP-07 | **D-124 The funding screen renders the server's fund position and never derives one:** `FundingView.tsx` loads `GET /funding/scholarships/:id/position` and shows received / committed / available beside the declared target; the browser-side `totalBudget − allocatedAmount` is removed, because after D-121 it subtracts a mirror from a target and describes no money. The screen also exposes the whole approved lifecycle (fund from a donation, apply to a named obligation, reverse, close) and invalidates the student/payment datasets an application changes | keep the client arithmetic; ship the API without an operator surface | PROVEN — three UI cases in `scholarship-funding-authority.test.ts`; the §35 freshness rule failed on the first version and passes now | Yes | Agent, under LAW 2, §28 and §35 |
| 2026-08-21 | WP-07 | **D-125 An instalment plan belongs to the term it pays (OWNER DECISION, implemented):** `student_installments` rows are children of the tuition obligation created for a semester, written through `PUT /api/students/:id/installment-plan` against that term. Paying an instalment settles that term with no operator choice of semester, which is what D-117 requires. The JSON `students.installment_plan` column is removed: it was a second, unowned authority that no foreign key could protect, and it is the reason an instalment payment settled no term at all | keep the JSON plan and add a semester to each element; require the desk to name the term per payment (D-117 rejects it) | PROVEN — WP07-F16 closed: an instalment payment now settles its term, and a term with 4,000 AFN paid by instalment refuses a 12,000 AFN cash collection with "Outstanding: 8000 AFN". Before the change, 14,000 AFN was collectable for a 10,000 AFN term. The plan may not promise more than the term bills, and a plan with a paid instalment can no longer be rewritten | Yes (A-1 holds) | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-126 The WP-03 instalment suite is re-expressed against the model that replaced the defect class:** the double-encoding cases become one case proving the profile no longer accepts a plan at all (and that the column is gone); structural validation moves to amounts and due dates, because identity is now a primary key; and four new cases pin plan-versus-term totals, term settlement, rewrite-after-payment and the unknown-instalment outcome | delete the suite; keep asserting JSON validation that no longer exists | PROVEN (10/10 in that file; full suite 2708 passed) | Yes | Agent, under §14, §70 and the D-112/D-115/D-123 precedent |
| 2026-08-21 | WP-07 | **D-127 An invoice declares what it bills, and only a tuition invoice touches tuition (OWNER DECISION D-118, implemented):** `invoices.purpose` (`tuition\|books\|exam\|other`, NOT NULL, CHECK) plus `invoices.obligation_id`, with a table CHECK making the obligation mandatory for `tuition` and forbidden for everything else. The purpose is the ONE input to two derived facts — the `payments.category` a collection is recorded under, and the term it settles — both resolved in `server/src/core/finance/invoicing.ts` and nowhere else. `fee`, the category the balance authority counts as tuition, is reachable from exactly one purpose, so nothing falls into it by default | per-LINE obligations (a partial payment would then have to be SPLIT between obligations by a rule that exists nowhere — inventing one is §61 business policy); keep `category = 'fee'` and special-case the exceptions | PROVEN — WP07-F17 closed. Before: a 3,000 AFN "Textbooks and stationery" invoice cut a 10,000 AFN tuition debt to **7,000**, and a 10,000 AFN tuition invoice paid in full settled the term at **0** so the desk answered `201` to a second collection. After: tuition reads 10,000 and 10,000 respectively, and the second collection is refused 400. 30 cases in `server/src/tests/work-packages/wp07/invoice-purpose-authority.test.ts` | Yes (A-1 holds) | **Owner** (D-118), implemented by Agent |
| 2026-08-21 | WP-07 | **D-128 A term may not be billed for more than it bills (WP07-F19 — found by ATTACK, not by report):** `tuitionBillingCapacity` = the term's net amount minus what is already settled minus the unpaid remainder of every non-cancelled tuition invoice already standing against it. A new tuition invoice may not exceed it; cancelling one returns its capacity. This is D-125's instalment rule ("a plan may not promise more than the term bills") applied to the other document that promises | leave it to each invoice's own balance check (which is what allowed the defect: every invoice was individually within its own remaining balance and nothing looked at the term) | PROVEN — the adversarial case created two 6,000 AFN tuition invoices on a 10,000 AFN term, paid both, and the term absorbed **12,000 AFN** (`creditBalance 2000`) before repair. After: the second invoice is refused with "That term has only 4000 AFN left to bill", a term already settled in cash refuses a new invoice, and cancelling releases the capacity | Yes | Agent, under LAW 4, §65 ATTACK and the D-125 precedent |
| 2026-08-21 | WP-07 | **D-129 Every invoice writer declares a purpose, and a document that bills nothing cannot take money:** all four writers were converted — the manual route (operator-declared), visitor conversion (`tuition`, naming the term the conversion creates, and its payment now carries that term instead of none), the extra-class path (`other`, plus the line item it never wrote), and the enrolment auto-invoice (`other`, forced — see A-18). `assertInvoiceHasLines` refuses payment on a line-less document at the money boundary, independent of every writer | fix only the manual route and let the other three keep defaulting to tuition | PROVEN — the extra-class fee no longer reduces tuition that was never charged; a line-less invoice answers 409 "has no line items and cannot accept payment"; full suite 2740 passed · 160 known skips; release gate 22/22 | Yes | Agent, under LAW 1 and D-118 |
| 2026-08-21 | WP-07 | **D-130 Five fixtures re-expressed, none deleted:** (i) two schema guards matched the bare word `purpose` as a proxy for the RETIRED budget-line model and now condemned the live `invoices.purpose` — both re-scoped to budget objects, keeping `is_marketing`/`mapping_status` global; (ii) `invoice-subsystem-integrity` bills `other` by default because it creates many invoices for one student and a term may not be billed twice (D-128), with the tuition path proven in the purpose suite; (iii) two conversion suites now delete the obligation chain before the term it points at (`student_obligations.semester_id` is ON DELETE RESTRICT); (iv) every fixture invoice declares a purpose. **Finding:** four fixtures used `INSERT OR IGNORE INTO invoices`, which silently swallowed the NOT NULL violation and surfaced as an unrelated payment-branch trigger error two statements later | delete the suites; weaken the NOT NULL or the CHECK so old fixtures keep passing | PROVEN (full suite 2740 passed · 160 known skips, 0 failed) | Yes | Agent, under §14, §70 and the D-112/D-115/D-123/D-126 precedent |
| 2026-08-21 | WP-07 | **D-131 A sponsorship promise settles nothing; received sponsorship money settles tuition (OWNER DECISION S6, implemented):** `sponsorship_agreements.monthly_amount` is a declared intent that bounds nothing — the same role `scholarships.total_budget` plays after D-121. What settles tuition is a `sponsorship_receipts` row: a donation from the **donor who signed the agreement** (enforced by `trg_sponsorship_receipts_donor_insert`), earmarked to it, then applied to a named tuition obligation as an `obligation_allocations` row with `source_kind = 'sponsorship'`. It writes no cash and no ledger row, because the donor's money was recognised when the donation arrived — the identical rule to D-120, for the identical reason | treat `monthly_amount` as money (a promise nobody paid would settle real debt); model a sponsorship as a scholarship fund (S5 rules them different concepts); give sponsorships their own settlement table (a second authority — LAW 1) | PROVEN — the active successor `server/src/tests/work-packages/wp09/sponsorship-money-authority.test.ts` (the historical authority had 31 cases): an agreement promising 5,000 AFN a month with no receipts settles nothing (400 "has received no money yet"); a funded one settles its term with branch cash, savings and the ledger row-count all unchanged and reconciliation healthy; it cannot exceed what it received, what the term owes, or reach a student it does not name | Yes (A-1 holds) | **Owner** (S6), implemented by Agent |
| 2026-08-21 | WP-07 | **D-132 One afghani cannot back two commitments:** `getDonationUnallocated` now subtracts both `scholarship_fundings` and `sponsorship_receipts` for the same donation. The two instruments compete for one remainder | count each instrument's earmarks separately (the same donation would then back a fund AND a sponsorship in full) | PROVEN — a 5,000 AFN donation with 4,000 allocated to a fund refuses a 2,000 AFN sponsorship receipt with "Only 1000 AFN of that donation is still unallocated", accepts 1,000, and reports 0 unallocated | Yes | Agent, under LAW 4 and D-121 |
| 2026-08-21 | WP-07 | **D-133 "Aid" is one SQL fragment, not six copies:** `AID_SOURCE_KINDS_SQL = ('scholarship','sponsorship')` is declared once in `server/src/utils/studentBalance.ts` and imported by the obligation authority. Six read paths — three in each module — ask "what has settled this without moving cash", and a copy that drifted would silently change what a student owes. `payment` is deliberately excluded: cash is attributed through `payments.semester` until E1b, and counting it here as well would settle every cash term twice | add `'sponsorship'` to each of the six queries by hand | PROVEN — a sponsored term reports settled through `getSemesterTuitionSettled`, `getStudentBalance` and the obligation position identically; full suite 2771 passed · 160 known skips | Yes | Agent, under LAW 1 and §13 |
| 2026-08-21 | WP-07 | **D-134 The sponsorship discount and the sponsorship agreement are documented and guarded as different things (OWNER DECISION S5, implemented):** the 100% `SPONSORSHIP` discount category reduces what the student is **charged** (no donor money exists; the institute forgoes revenue; owner authorization required), while a sponsorship agreement leaves the charge untouched and **settles** it with a donor's received money. Asserted rather than left to memory: applying a sponsorship leaves `student_semesters.net_fee_amount` unchanged and produces exactly one `sponsorship` allocation; `obligation_allocations` knows no discount and `sponsorship_agreements` carries no percentage | merge them; rename one to remove the collision (the owner ruled both names stay) | PROVEN — two cases at the end of `sponsorship-money-authority.test.ts` | Yes | **Owner** (S5), implemented by Agent |
| 2026-08-21 | WP-07 | **D-135 WP07-F18 is analysed before it is proposed (OWNER instruction):** per the owner's selection, the enrolment auto-invoice question produced a read-only assessment (`docs/work-packages/WP-07-F18-enrolment-invoice-analysis.md`) instead of an implementation. **It corrects the earlier scope estimate with measured evidence:** the defective combination needs `autoInvoice ≠ false` AND `writeSemester ≠ false`, and of the four production callers only the journey enrolment endpoint (`journeyRouter` `POST /enrollments`, mounted under `/api/students/:id/journey`) leaves both defaulted to true — the other three suppress one or both. No WP-05 suite asserts anything about the enrolment invoice (0 files match `invoiceId\|invoiceNumber` under `wp05`), and only 4 test files touch fee snapshots. The repair is therefore one endpoint, not a work package, and needs no WP-05 re-certification | implement the smallest symptom fix immediately; assume the pessimistic WP-05-wide blast radius recorded at F-17 checkpoint time | FACT (analysis performed at `f0b148d`; no source, schema or test changed; gate 22/22 at that commit) | Yes | **Owner** instruction, executed by Agent under §61, §62 and §105 |
| 2026-08-21 | WP-07 | **D-136 The enrolment term carries its tuition, and the enrolment bills one document per purpose (OWNER DECISION on WP07-F18, implemented):** `student_semesters` is created with `fee_amount` = the tuition the snapshot bills and `net_fee_amount` = that figure after discount, instead of a hard-coded `0`. The enrolment then issues a `tuition` invoice naming that term's obligation and a separate `other` invoice for the fees alongside it. Tuition receivable is single-sourced on the canonical authority for all three enrolment doors | keep one mixed invoice (a tuition charge on a document that can settle no term); make the mixed invoice `tuition` (it would credit a term that bills 0 and hand the student a phantom credit) | PROVEN — WP07-F18 closed. Before: the term billed **0** while the invoice billed 8,000, `getStudentBalance` reported no tuition debt, and one invoice existed instead of two. After: the term bills 8,000, the balance authority sees it, the tuition invoice names the term's obligation and paying it settles that term while the registration invoice's money does not. 17 cases in `server/src/tests/work-packages/wp07/enrolment-invoice-purpose.test.ts` | Yes (A-1 holds) | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-137 A retake fee is tuition (OWNER DECISION):** `TUITION_FEE_TYPES = ('semester','retake')`, declared once in `server/src/core/finance/invoicing.ts` and read by both the enrolment writer and the discount ceiling, so the two can never disagree about what tuition is. A repeat enrolment's retake fee joins the tuition invoice and the term's billed amount, and can be settled by scholarship or sponsorship money like any other tuition | treat `retake` as an administrative charge on the `other` invoice | PROVEN — a repeat enrolment bills 10,000 (8,000 + 2,000 retake) on its tuition invoice and on the term, with the registration fee still billed separately | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-138 A discount attaches to tuition only (OWNER DECISION):** the enrolment discount is validated against the TUITION total, not the whole fee snapshot, and the authorization ceiling in the journey route is a percentage of tuition rather than of the snapshot. This makes the enrolment match what visitor conversion and manual registration already do (`netTuition = gross - gross * percent / 100`, registration never discounted) — removing an inconsistency rather than introducing one. A fully discounted term still issues its tuition invoice, because the discounts-granted report sums `invoices.discount_amount` and dropping the document would erase the discount from the books | divide the discount pro-rata across both documents (a split rule that exists nowhere — the arithmetic rejected for per-line obligations in D-127); keep the ceiling on the whole snapshot | PROVEN — a 9,000 AFN discount that fits the 9,500 snapshot but not the 8,000 tuition is now refused; a 100% discount leaves `total 8000 / discount 8000 / net 0` on record | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-139 An enrolment bills tuition only against a term it created:** when a caller passes `writeSemester: false` it has already written its own term with its own tuition (manual registration and visitor conversion both do), so the enrolment bills only the fees alongside tuition — billing it again would charge one term twice. An enrolment that creates no term at all (no class) has no obligation to name and bills its whole snapshot as one `other` document; such an enrolment creates no tuition receivable in the balance authority, which is recorded as the narrow residual of WP07-F18 | bill tuition regardless (double-charges the two callers that own their own term); refuse to invoice at all when no term exists (silently drops a real charge and breaks the classless enrolment path) | PROVEN — an enrolment against a caller-owned term issues only the `other` invoice and the student's tuition due stays at the owner's single figure | Yes | Agent, under LAW 4 and D-127 |
| 2026-08-21 | WP-07 | **D-140 The enrolment discount is parsed, not coerced (WP07-F20 — found by ATTACK):** `assertMoney` replaces `Math.max(0, Number(input.discountAmount \|\| 0))` in `EnrollmentService.enroll`. The journey route already parsed its input, but every caller converges on the service, so the guard belongs there too | leave the parse at the route (any other caller of `enroll()` bypasses it) | PROVEN — before the repair `true` became a 1 AFN discount, `[1000]` became a 1,000 AFN discount, and `-1000` became a silent 0; all three are now refused with no enrolment, no term and no invoice written | Yes | Agent, under LAW 4 and the D-111/F-5 precedent |
| 2026-08-21 | WP-07 | **D-141 Cash settles a term by naming it (E1b, OWNER-approved scope, implemented):** every tuition cash writer — the payment desk (fee and instalment), the tuition-invoice payment, visitor conversion and the enrol-semester collection — writes an `obligation_allocations` row with `source_kind = 'payment'`, and `getSemesterTuitionPaid` reads those allocations instead of `payments.semester`. `getObligationPosition` keys on the obligation directly. Cash, scholarship and sponsorship money now settle through one authority with one shape. `payments.semester` is still written for display and refund attribution; retiring the column is the agreed follow-on once no reader remains | keep the free-text key (it cannot express which of two same-named terms was paid); migrate reads without writers (every existing payment would vanish from settlement) | PROVEN — 13 cases in `server/src/tests/work-packages/wp07/cash-allocation-authority.test.ts`, including the case that motivates the change: a student who takes "Term One", completes it and takes a second "Term One" now has each term report exactly what IT was paid, and the second term is collectable in full | Yes (A-1 holds) | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-142 A refund reverses the allocation it targets (OWNER DECISION on the E1b fork):** `refundPaymentAllocation` reverses the refunded payment's active allocation through the same mechanism scholarships and sponsorships use (`status='reversed'` with reason and actor) and, for a partial refund, writes a fresh allocation for the amount the student keeps settled. There is exactly one way to undo an allocation and `CHECK (amount > 0)` keeps guarding the table. A payment that settled no term has nothing to reverse and the call is a no-op | a signed negative allocation (two ways to undo one thing, and the positivity guard stops protecting scholarship and sponsorship money); refunds computed outside allocations (restores the duplication E1b removes) | PROVEN — a full refund re-opens the term to 10,000; a 3,000 partial refund leaves one reversed row and one active 7,000 row, re-opens exactly 3,000, and refuses 3,001; two successive partial refunds each reduce the term by exactly their amount with reconciliation healthy throughout | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-143 The payment desk keys a term's debt on the obligation, not the term's name (WP07-F21 — found by ATTACK):** both desk reads now resolve `getObligationPosition(...).outstanding` instead of `getSemesterTuitionSettled(studentId, semesterName)`. Precise allocations were not enough on their own: the desk still aggregated by NAME, so a student repeating a term had the first term's payments counted against the second | leave the desk summing by name (allocations would be exact while the only consumer that spends them stayed ambiguous) | PROVEN — with "Term One" paid in full, completed, and a second "Term One" opened, the desk answered `400 already fully paid` to a legitimate collection on the new term before the repair; it now collects all 10,000 | Yes | Agent, under LAW 1, LAW 4 and §65 ATTACK |
| 2026-08-21 | WP-07 | **D-144 The funding screen runs the sponsorship lifecycle and renders the server's position (S6 operator surface):** `FundingView.tsx` reads `GET /funding/sponsorships/:id/position` for every agreement and shows promised / received / applied / available side by side; a management modal records a receipt from a donation of the signing donor, applies money to a named tuition obligation, and reverses an application with a stated reason. The browser computes no figure. Applying or reversing invalidates `students`, `payments` and `funding`, because it settles tuition; a receipt invalidates `funding` alone, because it moves no tuition | ship the endpoints without a surface (server-complete, operator-incomplete — the state this slice existed to end); let the screen derive a position from `monthlyAmount` | PROVEN — 9 structural cases in `sponsorship-money-authority.test.ts`: 4 reproduced the absent surface before the work and now pass, 5 adversarial ones pin that the apply action is disabled while nothing is received, only the signing donor's donations are offered, a reversal requires a reason, and the promise is never rendered as the agreement's money | Yes | Agent, under LAW 2, §28, §35 and the D-124 precedent |
| 2026-08-21 | WP-07 | **D-145 An obligation reports settled AID, not settled "scholarship" (found by ATTACK):** `ObligationPosition.settledScholarship` and `getObligationScholarshipSettled` are renamed `settledAid` / `getObligationAidSettled`, and the field is renamed in the `/funding/students/:id/tuition-obligations` contract and the UI type. After D-131 the value includes sponsorship money, so the old name reported a donor's sponsorship as scholarship money to every reader of the contract | keep the name and document the discrepancy (a name that says something the value does not is exactly what §13 forbids) | PROVEN — behaviour is unchanged and the two suites that asserted the old field are re-expressed against the accurate one; full suite 2811 passed · 160 known skips | Yes | Agent, under §13 and LAW 2 |
| 2026-08-21 | WP-07 | **D-146 Revenue is attributed through the settlement authority, not guessed (WP07-F22):** `stmtRevenueByClass` and `stmtRevenueByTimeSlot` in `server/src/routes/bos.routes.ts` joined a payment to "the semester whose name matches, preferring the active one, else the student's most recent active semester". Both now read `obligation_allocations`: the payment names the obligation, the obligation names the term, the term names the class. One shared SQL fragment serves both reports, and only ACTIVE allocations count, so a refunded amount stops being reported as revenue for the class that no longer holds it | patch the name match to prefer a completed term (still a guess, and still silent when it is wrong) | PROVEN — WP07-F22: a student repeating "Term One" (completed in the Morning Class, running in the Evening Class) paid off the MORNING term, and the report credited the whole 10,000 AFN to the Evening Class — Morning read `undefined`. After: Morning 10,000, Evening 0; two terms paid separately report separately; a 4,000 refund drops Morning to 6,000. 5 cases in `server/src/tests/work-packages/wp07/revenue-attribution-authority.test.ts` | Yes | Agent, under LAW 1, LAW 4 and D-141 |
| 2026-08-21 | WP-07 | **D-147 The C-2 inventory count is corrected from 409 to 580 (OWNER-approved disposition):** the original figure counted each `it.each(...)` block as one case; executed, the 35 inventoried files run 580, all passing. The divergence concentrates in the table-driven suites (`invoice-subsystem-integrity` 32→71, `finance-money-writer-parity` 19→52, `discount-authorization-boundary` 19→40, `money-boundary-property` 10→28). Every prior statement of the C-2 obligation, including the one in this package's own certification record, understated it by 42% | close C-2 against the stated 409 (the obligation would be discharged against a number that was never true) | FACT (`npx vitest run <the 35 files> --reporter=json`; the count is recorded as derived, with the command that reproduces it) | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-148 `payment.test.ts` is retired as a skipped knowledge record:** it calls no application writer — the test body hand-writes an `INSERT INTO payments` and an `INSERT INTO financial_transactions` and asserts both rows exist, so it proves a property of its own SQL and no production path can regress and make it fail. The header names the five live suites that own what it gestured at. Skipped, never deleted (§14, §70; the D-85 precedent) | delete it; leave it running as apparent coverage it does not provide | PROVEN (2 cases now reported as skipped; suite 2817 passed · 162 skipped) | Yes (Git-recoverable) | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-149 Nine legacy suites are re-expressed so they discriminate the rules they claim to protect:** every one of them PASSED before this work and none was load-bearing. The largest was `money-boundary-property`, whose "inventory of every money-typed request field" was a hand-maintained list checked one-directionally — a new money field could never fail it, and `invoices.routes.ts` was absent from it entirely while validating three money fields. The inventory is now DERIVED from source across `routes/` and `core/finance/`, and the reverse direction is enforced: a money-shaped request field that reaches no boundary fails the build. Also re-expressed: `finance-money-writer-parity` and `money-writer-idempotency` (the four newer money writers had no parity or retry coverage — added beside the sponsorship authority), `journey-enrollment-discount-authority` and `payment-overpayment` (tuition-only fixtures made D-138's ceiling basis invisible), `invoice-subsystem-integrity` and `financial-integrity` (state the delegation of the tuition document to `invoice-purpose-authority`), `discount-cap` (scoped to the rule engine so its 30% no longer reads as the operative ceiling), `balance-single-source-of-truth` (an exact identity replaces `toBeCloseTo(..., 6)` on whole-AFN money) | leave them green and non-load-bearing | PROVEN BY MUTATION, twice: (i) adding an unvalidated `surchargeFee` request field to `invoices.routes.ts` left the old guard green and now fails it with `invoices.routes.ts: surchargeFee`; (ii) regressing the discount ceiling from the tuition total back to the snapshot total left the old fixture green and now fails 2 cases. Full suite 2817 passed · 162 skipped · 0 failed | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-150 One receivable, derived from the authorities (OWNER DECISION on WP07-F18b, implemented):** `GET /api/reports/overview` publishes `outstanding = { tuition, nonTuition, total, openInvoices }`. Tuition comes from the tuition authority (`getBranchOutstanding`) and from nothing else, so a term is counted once; everything that is not tuition is summed from the invoices that bill it, each floored at zero. The figure is a **position as at today** — it carries no date filter and no longer claims one, which the owner ruled is correct for a receivable | keep the invoice-derived figure and merely rename it (two receivable figures would still coexist); delete it (non-tuition receivable and the open-invoice count would be reported nowhere) | PROVEN — WP07-F18b closed: a term settled in full by a donor previously left its invoice `issued` and the report showed the whole term as owed forever, because aid settles an OBLIGATION and never touches the invoice. 5 cases in `server/src/tests/work-packages/wp07/receivable-authority.test.ts`, including that a tuition invoice is never double-counted (10,000 not 20,000) | Yes | **Owner**, 2026-08-21 |
| 2026-08-21 | WP-07 | **D-151 One definition of outstanding tuition, per student and per branch (WP07-F23 — found while implementing D-150):** `getBranchOutstanding` did not subtract aid while `getStudentBalance` did, so two implementations inside ONE module disagreed about the same student — the profile reported settled, the executive dashboard reported owing. Branch arrears were overstated by the full settled amount for every scholarship- or sponsorship-settled student. The branch query now subtracts active aid allocations through `AID_SOURCE_KINDS_SQL`, and the function takes `branchId: string \| null` so the organization-wide figure is the same query rather than a second one | add a separate all-branches function (a third implementation of one definition) | PROVEN — with 6,000 of a 10,000 term settled by a donor, the per-student figure read 4,000 and the branch figure read **10,000**; both now read 4,000, and both scopes respond identically to the same settlement | Yes | Agent, under LAW 1 and §13 |
| 2026-08-21 | WP-07 | **D-152 ERRATUM to the F-18b analysis:** the analysis tabulated the balance authority as "aid-aware: yes" and reported a probe showing the dashboard at 0 against the report at 10,000. That was **wrong in one respect**: the probe subtracted aid in its own arithmetic, and production's `getBranchOutstanding` did not. The real pre-repair state was that the dashboard ALSO read 10,000, and the disagreement was between `getStudentBalance` (0) and both branch-level figures. The defect was therefore wider than analysed, not narrower — recorded rather than quietly corrected, because an analysis that overstated its own evidence is exactly the failure the protocol exists to surface | silently amend the document | FACT (`git show HEAD:server/src/utils/studentBalance.ts` shows the pre-repair query with no aid subtraction) | n/a | Agent, under §103 |
| 2026-08-21 | Operations | **D-153 The durability sync opens a handle it is allowed to flush (Windows startup-backup failure):** `syncFile` opened each snapshot with a READ-ONLY handle (`open(filePath, 'r')`) and then called `handle.sync()`. Windows refuses `FlushFileBuffers` on a handle without write access and raises `EPERM: operation not permitted, fsync`, so the startup backup aborted before the backend finished initialising — and `syncFile` runs for the local daily snapshot, for every weekly/monthly tier and for every external copy, so all three paths failed. Opened `'r+'` instead. Deliberately **not** `'w'`, which truncates and would flush an empty file, destroying the backup it was called to make durable. Deliberately **no** try/fallback that skips the flush on error: a backup reported as durable without an fsync is a false guarantee | catch EPERM and continue without flushing (reports durability that was never achieved); switch to `'w'` (truncates the snapshot) | PROVEN — reported from Windows with `EPERM ... fsync` at `handle.sync()`, and the reporter's isolated probe returned `EPERM` for `'r'` and `FSYNC_OK` for `'r+'`. **POSIX permits fsync on a read-only descriptor**, verified on this platform (both modes return `FSYNC_OK`), which is why every Linux run passed and no behavioural reproduction is possible in CI — so the regression guard is structural and was mutation-tested: restoring `'r'` fails it | Yes | Agent, on an owner defect report |
| 2026-08-21 | WP-07 | **D-154 TR-4 is not discharged by the implementing agent, and no WP-07 certification is signed:** asked to obtain a genuinely independent review, the agent declined to produce one, because every slice, decision and test in WP-07 was authored by it and a self-authored review is the exact risk TR-4 records. Instead it ran the repository's pre-existing mutation harnesses — artifacts it did not write, whose verdicts do not depend on its judgment — and prepared a reviewer dossier. Three findings are recorded (TR4-F1/F2/F3) and five recommendations (TR4-R1..R5) are **recorded and deliberately not implemented**, pending owner-approved review | write a self-review and call TR-4 mitigated | FACT — and evidenced against itself: two self-authored artifacts in this one session were green and wrong (the F-18b analysis, D-152; and the first C-2 money-boundary guard, whose `\b` word boundary never matched `surchargeFee`). **Objective machine findings:** no gate runs any of the 18 mutation harnesses; `funding-amount` has 3 live survivors including the original defect, proven PRE-EXISTING (`git diff d29554b..HEAD` empty for both the suite and the handler); `finance-money-writer` has 6 survivors with causation UNDETERMINED and explicitly referred to the reviewer rather than resolved by the author | n/a — nothing implemented | Agent, under LAW 6 and §105, awaiting **Owner** |
| 2026-08-21 | Operations | **D-155 The mutation harnesses are executed by a gate (TR4-R1, owner-approved):** `server/scripts/run-mutation-harnesses.mjs` runs all 18 harnesses, reads the **process** exit code, prints every survivor and exits non-zero if any harness fails. Exposed as `npm run audit:mutation`. The CI job that runs it **could not be pushed by the agent** — `.github/workflows/*` is rejected for this identity without `workflows` permission — so the job definition is handed to a human in `docs/work-packages/WP-07-TR4-ci-mutation-job.md`. Harness assertions and `EQUIVALENT` sets are **untouched** — no survivor was classified to make the gate green. Kept OUT of `release:validate` deliberately: it takes ~6 minutes and is currently red for pre-existing survivors, so folding it in would block unrelated work and create pressure to silence a survivor | add it to `release:validate` (blocks everything, invites silencing); classify the current survivors as equivalent to get a green gate | PROVEN — first authoritative run: **9 passed · 9 failed · 32 surviving mutants**, of which **22 are undocumented and now recorded as UNRESOLVED**. It also fixes a method defect: piping a harness into `tail` returns tail's status, which made three failing harnesses look green earlier in this session | Yes | **Owner** (TR4-R1), implemented by Agent; CI wiring pending a human |
| 2026-08-21 | WP-07 | **D-156 TR-4 Stage 1 complete; 22 mutation survivors recorded as UNRESOLVED:** per the owner's staged-review instruction, Stage 1 examined artifacts and machine verdicts only, with the decision register deliberately not consulted. Every survivor is unresolved until independently classified — the implementing agent classified none. Causation is reported as `git diff` fact only: `funding-amount`'s F1/F3/F7 **cannot** be WP-07's doing (neither the suite nor the handler changed since `d29554b`); every other harness is undetermined and referred to the reviewer. New finding **TR4-F5**: `rbac-authorization` reports 7 INVALID mutants — patterns that no longer match, so those mutants were never applied and the harness's killed-ratio overstates RBAC coverage | let the author classify the survivors; report the killed-ratio without noting the invalid mutants | FACT (`npm run audit:mutation`, run on a verified-healthy tree; an earlier run was discarded as invalid because the sandbox had pruned `node_modules` and 8 harnesses aborted on an un-green baseline) | n/a — nothing implemented | Agent, under LAW 6, awaiting **Owner** and a reviewer |
| 2026-08-21 | WP-07 | **D-157 TR-4 Stage 2 — artifacts reviewed before rationale; four findings, none repaired:** the decision register was deliberately not read for this stage. Four WP-07 authorities were mutated directly: term billing capacity, aid-aware branch arrears and revenue attribution were all **KILLED**, so those authorities have tests that genuinely fail when broken. The fourth **SURVIVED**, and the investigation found why — `getSemesterTuitionSettled` is imported by `students.routes.ts` and **never called**, so the semester-name-keyed settlement path has zero production call sites since the payment desk moved to obligation-keyed reads. Deleting its filter changes nothing any production path observes (**TR4-F6**, dead code in a money authority, §44). Also recorded: `TUITION_NET_SQL` is exported so the rule is written once, then inlined four more times inside its own module (**TR4-F7**); the "active cash allocation" predicate is expressed three times while the aid predicate was consolidated (**TR4-F8**); and `no-unused-vars` is a **warning**, so the green gate admitted the dead import and ~100 other unused values (**TR4-F9**) | repair the findings during the review; let the author decide whether the surviving mutant is equivalent | PROVEN — mutations executed with every file restored (`git status` clean); `grep -n "getSemesterTuitionSettled(" src/routes/students.routes.ts` returns no matches; `npx eslint` reports the dead import as a warning the gate ignores | n/a — nothing implemented | Agent, under §44 and LAW 1, awaiting **Owner** and a reviewer |
| 2026-08-22 | WP-07 | **D-158 TR4-R9 — the dead name-keyed settlement path is removed (supersedes the authority subjects of D-116, D-133 and D-141):** `getSemesterTuitionPaid`, `getSemesterScholarshipSettled` and `getSemesterTuitionSettled` had zero production call sites since the payment desk moved to obligation-keyed reads (D-143); the money authority that still decides what a term owes is `getObligationPosition` → `getObligationCashSettled`/`getObligationAidSettled` in `server/src/core/finance/obligations.ts`. The living registries were re-pointed in the same change; this row records the supersession in the rationale record | keep exported dead readers in a money authority because tests still called them | PROVEN — removal executed with the full lifecycle (release 22/22, suite green) in commit `04ece70`; evidence: `docs/work-packages/WP-07-TR4-stage2-artifact-review.md` (TR4-F6) and the re-pointed rows in `docs/registries/canonical-authority.md` | No | Agent, under Owner approval recorded in `docs/work-packages/WP-07-TR4-survivor-disposition-decision.md` §5–§8 |
| 2026-08-22 | WP-07 | **D-159 TR4-R12 — the lint gate fails on unused imports/variables (error, not warning):** `no-unused-vars` is `error` in both `eslint.config.js` and `server/eslint.config.js`; the ~100 pre-existing violations were cleared. A dead import in a money authority had entered the tree through a green gate (TR4-F9) — the gate now proves reachability, not just compilation | keep the warning and rely on review | PROVEN — both lint gates green since commit `04ece70`; the gate later caught two unused imports introduced during harness surgery and they were fixed before commit | No | Agent, under Owner approval (same record) |
| 2026-08-22 | WP-07 | **D-160 TR-4 M7 disposition — the subsumed `hasPermission` leg is removed (Owner Option b), never classified EQUIVALENT:** at both production call sites the guard was `!hasPermission(ctx, code) || !canAccessBranchForRequirement(…, {permissionCodes:[code]})`, and the branch leg resolves from the same post-deny `ctx.permissions` with strictly stronger conditions, so it implied the set-membership test. The redundant leg was removed from `requirePermissionAtBranch` (`server/src/routes/security.routes.ts`) and `requireRoleAssignmentAuthority` (`server/src/routes/users.routes.ts`) and the now-unused `hasPermission` was deleted from `server/src/core/rbac/rbac-service.ts`; `hasAnyPermission` remains the single set-membership authority. The mutant M7 then retired through the OBSOLETE mechanism with the subsumption proof as evidence | classify M7 EQUIVALENT without changing the code; keep both legs expressing one rule twice | PROVEN — behaviour-preservation verified by 206/206 wp02 + organization-authorization tests passing; the deny-path suite wiring added in Bucket 2 is preserved; commits `8cba3e4`, evidence in `docs/work-packages/WP-07-TR4-survivor-disposition-decision.md` §7 | No | **Owner** (Option b), executed by Agent |
| 2026-08-22 | WP-07 | **D-161 TR4-R14 and the survivor dispositions — the mutation gate is fully green and nothing is unclassified:** harness measurement integrity was repaired (44 stale anchors re-based in total under the TR4-R14 discipline, vacuous target suites re-pointed with void-detection, INVALID surfaced in the gate summary); the 13 unclassified survivors were disposed by execution (10 EQUIVALENT with written diffs, fmw M2/M4 and bos B5 killed by error-contract pins, J10 via an executed probe); 5 mutants retired OBSOLETE with evidence (rbac M1/M7/M12, teacher-update M14, security S10) and 2 stale EQUIVALENT entries corrected to their executed truth (branch-profile F11, security S9). Final state: 18 harnesses · 0 failing · 0 unclassified survivors · 0 INVALID; every survivor line is a documented registry member | classify survivors silently; leave anchors stale and the gate red; weaken assertions to fake kills | PROVEN — `npm run audit:mutation` 18 passed · 0 failed · 18 surviving (all documented) · 5 obsolete · 0 INVALID; `npm run release:validate` 22/22; suite 2842 passed · 162 skipped; evidence: `docs/work-packages/WP-07-TR4-mutation-measurement-checkpoint.md`, `docs/work-packages/WP-07-TR4-independent-review-verdicts.md` §5–§8, commits `3064c82`–`717cebc` | No | Agent, under Owner approvals recorded in the same documents |
| 2026-08-22 | WP-07 | **D-162 TR-4 handoff executed — the reviewer package is transferred to a genuinely independent reviewer, and every author-made disposition is downgraded to a claim pending that verdict:** the Owner ruled (2026-08-22): proceed with TR-4's independence requirement, no self-review, no §R exception. The implementing agent therefore performed no further review: it re-measured the handoff baseline on branch `arena/01a0299b-toefl-house` at `102074c` (release gate 22/22; suite 2844 passed · 162 skipped · 0 failed; mutation gate 18/0 with 18 documented survivors and 5 obsolete, all exit codes read from the processes), wrote the handoff record `docs/work-packages/WP-07-TR4-handoff-to-independent-reviewer.md` around `docs/work-packages/WP-07-TR4-independent-reviewer-package.md`, and stopped. All dispositions in `docs/work-packages/WP-07-TR4-independent-review-verdicts.md` §5–§8 were applied by the implementing agent and do not discharge TR-4; the independent reviewer must verify or refute each by its own execution before any disposition is final. No §R entry was added and none will be sought | self-review the remaining questions; seek a §R exception to §71; certify WP-07 on author-made verdicts | FACT — gates re-executed at handoff with the exact commands in the handoff record §3; handoff record, branch push and tracking issue recorded therein | n/a — nothing implemented | Agent, executing the **Owner** ruling of 2026-08-22, under §71, §103 and §105; awaiting the independent reviewer's execution-backed verdict |
| 2026-08-22 | WP-08 | **D-163 Payroll facts are linked immutable events with explicit correction:** a teacher or employee payment has one positive, canonical-Shamsi ledger fact linked to one matching expense transaction and one payroll envelope debit. A correction is only `posted → voided` with an operator, reason and signed contra transaction; a voided idempotency key is not a live event and may be reused by the new correction. Payroll reports date posted ledger facts through the linked accounting transaction, never through an unbounded timestamp string. Employee correction uses the same lifecycle as teacher correction. | permit raw ledger edits/deletes; silently fall back to neutral policy; retain employee status columns without a correction command; report by `paid_at` text | PROVEN — WP-08 attack 9/9 plus final authority-audit 1/1 and cold-review 4/4 then 1/1 failures were repaired; `payroll-certification.attack.test.ts` and `payroll-certification.review.test.ts` now cover replay boundary, correction, rule failure, report reconciliation, direct-write/link/delete attacks and UI contract; fresh-DB full suite 2863 passed / 162 known skips after final repair | Yes (canonical schema/index/trigger changes; no production data) | Agent, under LAW 1, LAW 3, LAW 4, LAW 5, LAW 6, §49, §68–71 and the WP-08 checkpoints |
| 2026-08-22 | WP-05 | **D-164 Course-offering levels are explicitly versioned, never inferred:** an unversioned level is allowed only as an explicit shared/staged catalog fact. It cannot be selected for a course offering. A new level may name a compatible non-archived program version, and an existing unversioned level may be attached once through `POST /api/academic/levels/:id/assign-version`; a versioned level is not silently moved by generic update. The UI exposes version identity, exact offering filtering and the existing academic-freshness authority, so the natural Program → Version → Level sequence no longer produces a false incompatibility | relax offering validation for NULL version; infer current/default version; force delete/recreate; allow generic PUT to silently move a level | PROVEN — fresh-database reproduction failed 4/4 before the repair: program/version/term/branch were valid, but level creation stored NULL and offer creation returned 400. `course-offering-version-compatibility.test.ts` now passes 5/5, including actual offering creation and preview → draft → publish class generation; 178-file / 2868-case server suite plus release validation 22/22 pass after this remediation | Yes (API/UI contract only; no schema migration or bulk mutation) | Agent, under D-86, LAW 1, LAW 3, LAW 6, §49 and the WP-05 remediation checkpoint |
| 2026-08-23 | WP-09 | **D-165 Restricted donations have one structured, mandatory target (OWNER DECISION):** a restricted donation names exactly one Campaign, Scholarship or Sponsorship identity; a free-text note is never a substitute. The initial funding fact is created atomically in that target’s permitted path and every later allocation enforces the target. | treat `restricted` as an informational Boolean; accept free-text purpose; infer a target from the current/default campaign | DECISION (owner, 2026-08-23) | Yes (canonical schema/API redesign; no production data) | **Owner** |
| 2026-08-23 | WP-09 | **D-166 Donor/campaign impact is exact provenance, never pooled attribution (OWNER DECISION):** a donor/campaign report may claim only donations and aid applications whose source graph explicitly reaches that donor/campaign. Branch-wide figures, aggregate fund balances, inferred provenance and missing source links are excluded rather than attributed. | show all branch metrics under a donor/campaign label; proportionally allocate pooled fund effects; silently infer donor/campaign from a current association | DECISION (owner, 2026-08-23) | Yes | **Owner** |
| 2026-08-23 | WP-09 | **D-167 Impact is derived-only (OWNER DECISION):** manual `impact_metrics` and `success_stories` are not operational or reporting truth and are retired after the deletion protocol. Impact reports are immutable derived snapshots from canonical operational facts. | build a parallel manual metric/story workflow; retain unused tables/endpoints as optional future capability | DECISION (owner, 2026-08-23) | Yes (no production data) | **Owner** |
| 2026-08-23 | WP-09 | **D-168 Funding/Impact authorization is permission based and financially separated (OWNER DECISION):** Global Owner is unrestricted; Donor Manager and General Manager manage donors, campaigns, aid/funding allocations and Impact; Finance Manager may register donations only and has neither `Funding.Edit` nor Impact mutation authority. Routes must consume permission/RBAC scope, never role-name gates. | retain role-name guards; give Finance broad Funding edit authority; make mutation authority a frontend visibility rule | DECISION (owner, 2026-08-23) | Yes | **Owner** |
| 2026-08-23 | WP-09 | **D-169 Unspent terminal sponsorship money returns to its linked campaign as a restricted monetary allocation fact (OWNER DECISION):** terminal transition returns every unused received source to the agreement’s valid campaign with amount, actor, reason, audit and reconciliation. It never becomes unrestricted cash and no valid campaign blocks terminal transition until explicit resolution. | leave money locked on a terminal agreement; silently release it to general funds; refund it without a defined donor-return policy; infer another agreement | DECISION (owner, 2026-08-23) | Yes | **Owner** |
| 2026-08-23 | WP-09 | **D-170 A campaign return is an immutable non-cash funding fact, not new ledger income or expense:** donation cash is recognized once through its linked `financial_transactions` row. Returning a terminal sponsorship’s unused receipt balance to its campaign writes `campaign_funding_entries` with original donation, receipt, actor and reason; writing a second cash transaction would double-count the same AFN. | record a second income/expense row; silently unlock the donation; mutate the original restriction | PROVEN — source-aware return, cash-preservation and reconciliation assertions in `server/src/tests/work-packages/wp09/sponsorship-lifecycle-guard.test.ts` and `sponsorship-money-authority.test.ts` | Yes | Agent, implementing D-169 under LAW 1 and LAW 4 |
| 2026-08-23 | WP-09 | **D-171 An aid application names the received source that funded it:** scholarship applications carry `scholarship_funding_id`; sponsorship applications carry `sponsorship_receipt_id`. A source can neither be inferred from aggregate fund availability nor shared beyond its received amount. This makes donor/campaign impact exact and keeps allocation capacity database-enforceable. | aggregate-only award/agreement source; proportional donor attribution; FIFO inference | PROVEN — source, direct-write, report and concurrent-source attacks in `server/src/tests/work-packages/wp09/funding-impact-authority.attack.test.ts` | Yes | Agent, implementing D-166 under LAW 1, LAW 3 and LAW 6 |
| 2026-08-23 | WP-08 dependency remediation | **D-172 Salary-status accepts the established `monthName` query spelling as well as `month`:** WP-09 verification exposed `teacher-history-integrity.test.ts` failing because salary payment wrote the named period but `GET /salary-status?monthName=…` silently read the current period. The route now resolves either spelling through the same canonical payroll-period parser and rejects a conflicting pair. This is a narrow blocking repair only; it does not reopen the WP-08 architecture or certification scope. | leave the false current-period read; change the historical/API contract test to hide it; add a second period parser | PROVEN — `server/src/tests/teacher-history-integrity.test.ts` passes after the repair | Yes | Agent, under the user’s WP-08 blocking-dependency exception and LAW 6 |
| 2026-08-23 | WP-10 | **D-173 Canonical Books facts:** under the Owner’s D-16 issuance requirement, Book commerce is one Book-domain sale fact linked to one payment and one income transaction; generic Student payment is not a second Book writer. Inventory availability is derived from immutable receipts, active sales and active student loans, never a mutable stock mirror. Lending is student-only within the actual existing Book evidence, has explicit due dates and no invented fee/deposit/replacement accounting. Catalog history is archived rather than deleted, and every Books command consumes permission-based RBAC. | preserve `books.stock` plus independent student payment/sale writers; encode loans as zero-price sales; infer lending fines/terms; role-name guards | DECIDED — WP-10 plan `docs/work-packages/WP-10-books-plan.md`; implementation and direct attack evidence required before certification | Yes (schema, finance, RBAC and removals; no production data) | Agent, implementing D-16 under LAW 1, LAW 3–6, §20–23, §49 and the Owner’s current autonomous-completion scope |
